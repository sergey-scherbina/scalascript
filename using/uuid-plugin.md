# Claim: uuid-plugin

Agent: Claude (this session)
Claimed: 2026-06-04
Task: Implement uuid-p1 + uuid-p2 + uuid-p3 as specified in docs/specs/uuid.md.
      uuid-plugin with UuidIntrinsics (JVM + JS), uuid.ssc stdlib module,
      build.sbt wiring, UuidPluginTest (format + ordering + parse/validate).
