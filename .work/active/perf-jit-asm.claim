feature/perf-jit-asm 2026-07-10
agent: claude (lucky-perch)
status: in-progress
scope: WIDE JIT foundation (Sergiy-directed, strategy C) — spec specs/wide-jit-typed-input.md.
DONE + on main:
  C-1 (dbec2af53) Typer records Term->SType node-type map (nodeTypes).
  C-2 (ee661c949, 2026-07-12) interpreter runs the ORIGINAL ast.Module via
    InterpreterBackend.compileAstModule (skips Normalize->Denormalize round-trip + re-parse in
    compileViaBackend for the interpreter backend). VERIFIED behaviour-neutral: full conformance
    corpus swept through `ssc-tools run --v1` with C-2 active (marker-confirmed) — all 146
    INT-eligible cases identical to expected; the 16 non-matches are all non-INT-eligible
    (backends:[jvm]/[js]/[node]/[v2] / requires:HttpClient), fail on missing v2/jvm features
    unrelated to the Normalize bypass.
next: C-3 — typecheck the original module once, thread nodeTypes to VmCompiler.compile via a
  new closure (like metaFor), seed regType from it. Then C-4 (remove type-unknown JIT bails)
  + C-0 (baseline JIT miss histogram, SSC_JIT_STATS=1, foreground sbt).
