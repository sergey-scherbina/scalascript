feature/v2-f4-reflip-D1 2026-07-23T08:13:07Z
agent: opus-orch
heartbeat: 2026-07-23T11:05:00Z
status: VERIFICATION-COMPLETE (claim OPEN — orchestrator gates release on CI-green)
done-so-far: D1 NOT applied — superseded by D2 (84712131d/cca93b867: F runner emits IrProg Data directly + Reader-free validateNoReader → zero ssc.Reader; whitelisting per D1 would re-introduce that load = worse). Re-flip already LANDED (10bb15bfa, frontIsF opt-out). Docs already done (spec §7 step4, CHANGELOG). INDEPENDENT VERIFICATION (all GREEN): plugin-boundary + backend-isolation smokes PASS under F-default; ZERO ssc.Reader class-loads; guard proven fail-loud (fires on ssc.Reader/scalascript.parser.Parser/scala.meta, silent on clean); semantic 248/248; X1 fixpoint byte-identical 405396 B (observed); F-sensitive e2e A/B (19 native/v2 smokes) 14 ok / 5 both_fail(env,symmetric) / 0 F_REGRESSION at script level.
FOUND 1 REAL out-of-corpus F regression (documented multi-file caveat class): `ssc run --native A.ssc B.ssc` runs multiple POSITIONAL files in REVERSE order under F (second,first) vs correct legacy (first,second), deterministic 3/3. Blast radius SMALL (single-file + imports byte-identical F-vs-legacy). NOT in CI (v21-native-entry-smoke), MASKED in whole-script rc-A/B (both_fail: F fails L48, legacy fails later on env) — the check-level L48 divergence is the signal. Recorded minimal repro BUGS.md cf23c5feb. F4a fallback can't catch it (wrong value, no error).
next (orchestrator decision): fix-forward F multi-file positional-args ordering (deep, F5b arc) OR accept the documented caveat OR revert the reversible one-liner. CI in_progress on 10bb15bfa (release gate) — note it will NOT catch this regression (entry-smoke not in CI). Interim workaround: SSC_FRONT=legacy for multi-positional-file runs.
