---
name: v21-native-typed-pattern-boundary
---

[IndentContext, Wrapper, Other, level, classify](typed-pattern-boundary-lib.ssc)

# Imported typed pattern boundary

```scalascript
println(level(Wrapper(3)))
println(classify(Wrapper(3)))
println(classify(Wrapper(1)))
println(level(Other(9)))
```
