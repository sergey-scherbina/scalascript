#!/usr/bin/env bash
# ssct-hm — type INFERENCE (Hindley-Milner / Algorithm W) as a tool. An ssc0 program
# (bin/ssct-hm.ssc0) run on the ssc VM: read UNANNOTATED source text -> lex+parse (ssc0)
# -> infer a principal type (ssc0) -> print. Pipeline: text -> (ssc0 front) -> Term -> hm.
set -euo pipefail
DIR="$(cd "$(dirname "$0")" && pwd)"
exec command scala-cli run "$DIR/src" --server=false --quiet -- run "$DIR/bin/ssct-hm.ssc0" "$@"
