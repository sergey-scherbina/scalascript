---
name: v21-native-typed-pattern-boundary
---

[IndentContext, level, classify](typed-pattern-boundary-lib.ssc)

# Imported typed pattern boundary

```scalascript
println(level(IndentContext(3)))
println(classify(IndentContext(3)))
println(classify(IndentContext(1)))
```
