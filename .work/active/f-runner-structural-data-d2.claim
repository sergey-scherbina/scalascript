feature/f-runner-structural-data-d2 2026-07-23T00:00:00Z
agent: opus
heartbeat: 2026-07-23T05:00:00Z
status: in-progress
done-so-far: Sergiy chose D2 for flip blocker f-native-out-of-corpus-smoke-regressions ③.2 (F runner class-loads ssc.Reader via #coreir.decode -> isolation smokes RED). D2 = F runner emits structural Data DIRECTLY, no text->decode round-trip, so zero ssc.Reader at native runtime. MEASURED: legacy runner (ssc1-run.ssc0:651) calls lowerProg(allStmts) which returns IrProg Data in-process (no Reader); F runner (ssc1-run-fsub.ssc0:680) does #coreir.decode(#coreir.eval(lowerProg(fProg))) where F's compile emits IR TEXT then decode(Reader) parses it.
next: add a self-hosted S-expr->IrProg-Data reader in fsub.ssc (23 IR node tags: IrProg/IrDef/IrApp/IrLam/IrLet/IrLetRec/IrIf/IrCtor/IrMatch/IrArm/IrPrim/IrWhile/IrSeq/IrLit + IrInt/IrBig/IrFloat/IrBool/IrStr/IrBytes/IrUnit + IrLocal/IrGlobal); runner calls compileToData = irTextToData(compile(src)) via #coreir.eval -> Data directly, drop #coreir.decode. Verify: v21-native-plugin-boundary + v21-plugin-backend-isolation smokes green under F (no ssc.Reader loaded); ALL v21-native-*/self-hosted smokes A/B green; X1 fixpoint byte-identical (F text unchanged); semantic 248/248; dualrun 45/45. COORDINATE with opus-orch (owns flip + active v2-f5c-removal kernel lane); fsub.ssc shared.
