slug: v2.2-p6.5-env
scope: literal P6.5 — a compiler in the subset with a proper lexical ENVIRONMENT + let-bindings. env is a
       list of names (innermost first), a variable resolves to (local idxOf); `let x = e1 in e2` desugars
       to (app (lam 1 e2-with-x) e1). Reads real Scala syntax, emits executable CoreIR (foundation for
       match/nested scopes). def f(x) = let y = x + 1 in y * y; f(4) -> 25.
files: uniml/core/src/test/.../spike/**, specs/v2.2-*, SPRINT.md
branch: feature/v2.2-p6.5-env
