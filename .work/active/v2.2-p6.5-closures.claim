slug: v2.2-p6.5-closures
scope: toward P6.5 — (1) close the block-body-in-match-arm grammar gap (parseArm uses parseExpr not
       parseBlock; parseBlock must also stop at `case`/`}`); (2) strengthen the self-host proof with a
       higher-order lambda-calculus interpreter WITH CLOSURES written in the subset (meta-circular
       evaluator: closures capture environments, application extends them). Byte-identical + runs.
files: uniml/core/src/test/.../spike/**, specs/v2.2-*, SPRINT.md
branch: feature/v2.2-p6.5-closures
