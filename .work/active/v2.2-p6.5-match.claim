slug: v2.2-p6.5-match
scope: literal P6.5 — a compiler in the subset with pattern MATCH + list/constructor data. Uppercase idents
       are constructors (Nil -> (ctor Nil), Cons(a,b) -> (ctor Cons a b)); `e match { case Nil => .. case
       Cons(h,t) => .. }` -> (match e ((arm Nil 0 ..) (arm Cons 2 ..))) with pattern vars pushed onto the
       env (reversed slots). Builds on selfhost-env. sum([7,8,9]) -> 24.
files: uniml/core/src/test/.../spike/**, specs/v2.2-*, SPRINT.md
branch: feature/v2.2-p6.5-match
