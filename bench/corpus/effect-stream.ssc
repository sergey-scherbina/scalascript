# effect-stream

Effect dispatch + Source allocation — Stream.emit in a loop.

```scalascript
def workload(): Int =
  val (src, _) = runStream {
    var i = 0
    while i < 10000 do
      Stream.emit(i)
      i = i + 1
  }
  val lst = src.runToList()
  lst.length
```
