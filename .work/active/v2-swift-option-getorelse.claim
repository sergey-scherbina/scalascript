feature/v2-swift-option-getorelse 2026-07-13T00:00:00Z
agent: claude-code (bold-quail)
heartbeat: 2026-07-13T00:00:00Z
status: in-progress
scope: fix a real runtime crash found actually RUNNING busi's real app.ssc
  natively (not just compiling it) after the v2-swift-coreir-sexpr-embed fix
  landed: SwiftRuntime.scala's method() dispatcher has no case for
  None/Some.getOrElse, so it falls through to the generic "unimplemented
  method" Op-deferral stub, and the entry term's `if` on that Op value fails
  with "if: condition not Bool". Fix mirrors the existing, working JVM/general
  interpreter semantics at v2/src/Runtime.scala:2771-2772 exactly.
next: add explicit Some/None getOrElse handling in SwiftRuntime.scala's
  method() dispatcher, verify against busi's real app.ssc (fast raw-swiftc
  harness, no crash + real evaluated output), run v2SwiftBackend/test, commit,
  push, release claim.
