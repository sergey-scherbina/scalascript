feature/v2-backend-wasm 2026-07-13T00:00:00Z
agent: claude-code (bright-shrew)
heartbeat: 2026-07-13T00:00:00Z
status: in-progress
scope: add WASM as a 4th Phase-4 v2 backend target, alongside JVM/JS/Rust
  (v2/backend/{jvm,js,rust}). Sergiy: "Реализуй wasm для ssc v2".
  v2/backend/check.sh is currently "ALL GREEN (N fixtures x 3 backends)";
  there is no wasm row there yet. A working WASM path already exists at the
  SELF-HOSTED ssc0 layer (v2/ssc0-wasm, v2/conformance/check.sh, gated on
  wasm32-wasip1 + node:wasi, confirmed working) but NOT at the Phase-4
  Scala-CoreIR-generator layer that v2/backend/check.sh covers — that's
  the actual gap. specs/v2-rust-wasm-lanes.md already documents and warns
  not to confuse these two layers.
next: design is settled (no new codegen needed — WASM is a cross-compile
  TARGET of v2/backend/rust/RustBackend.scala's existing Rust output, not a
  new value-representation/dispatch backend): write
  v2/specs/63-backend-wasm.md, add run_wasm() to v2/backend/check.sh
  (rustc -O --target wasm32-wasip1 + v2/scripts/run-wasi.mjs, reused
  unchanged, toolchain-gated so it skips gracefully without wasm32-wasip1
  installed), verify real green run, commit, push.
