slug: owner-decisions-record
branch: feature/owner-decisions-record
agent: opus
started: 2026-07-27T18:20:00Z
heartbeat: 2026-07-27T18:20:00Z
direction: Coordination / specs (Sergiy's decisions, 2026-07-27)
scope: |
  Sergiy answered the four open decisions. Record them DURABLY first (they gate two arcs and one
  process rule, and every agent is affected), then implement in priority order.
  DECIDED:
   1. portable-capsule-integrity -> (c) adopt the host format-v3 model (HMAC + audience/tenant/quota)
      so both lanes promise the same thing. Unblocks vector-15 E4.
   2. vector 26 D1 -> a DISTINCT `TooLateToCancel` failure (not collapsing into AlreadyResumed).
   3. vector 26 D4 -> cancellation checked FIRST in the §11.1 admission order (caller intent wins).
   4. AGENTS.md §4c -> RELAX the exact-SHA CI rule (see BUGS ci-runs-cancelled-under-churn: 6/14
      runs cancelled, queue 8 deep, exact-SHA verdict unreachable at 4 agents).
  ASSUMED (my recommendations; Sergiy did not name them — flagged, not silently taken):
   D2 keep base contract portable + a MANDATORY capability bit so "cancel does not stop in-flight
      work" is machine-readable; D3 host-local + name it honestly (cancelLocally); D5 `Cancelled` /
      code CANCELLED / tryCancel internal.
  This claim = the RECORD + the §4c change only. The two implementation arcs get their own claims.
