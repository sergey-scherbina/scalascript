#!/usr/bin/env ssc
---
name: nfc-ndef
version: 0.1.0
description: NFC NDEF capability and record-constructor demo via std.nfc
backends: [int]
---

<!--
`backends: [int]` — owner's decision, 2026-07-31. NOT because the other lanes are broken:

  js  declares `Unsupported(NfcNdef,js)` — an explicit capability signal, verified by running it.
  v2  runs the whole example correctly since NFC joined the standard graph, and differs on exactly
      ONE line: it prints `NFC platform:  jvm-host` where the interpreter prints `interpreter`.
      Both constants are hard-coded AND asserted by their own unit tests
      (`NfcIntrinsics.scala:100`, `NfcNativePlugin.scala:68`), so this case can never match an
      int-derived golden while it prints `caps.platform`.

The divergence is REAL and does not disappear with this declaration — it is recorded in
`v2/BUGS.md` (`nfc-platform-string-differs-by-design-so-the-row-cannot-go-green`) and pinned by
`tests/e2e/v21-standard-nfc-smoke.sh`, which runs this example on the native lane, checks every
other line against the interpreter, and fails if either side changes its platform string. Removing
this case from the v2 column does not remove that coverage.

⚠️ FLOW form on purpose: `contract.sc:619` accepts `backends: [int]` and SILENTLY IGNORES the block
form, which would look like a declaration and gate nothing.
-->

# nfc-ndef - std.nfc demo

Demonstrates portable NFC capability checks and NDEF record construction.
Run with: `ssc run examples/nfc-ndef.ssc`

```scalascript
[nfcCapabilities, nfcPermissionStatus, requestNfcPermission, readNdef, textRecord, uriRecord, mimeRecord, NfcScanOptions](std/nfc.ssc)

val caps = nfcCapabilities()
println("NFC platform:  " + caps.platform)
println("NFC supported: " + caps.supported)
println("NDEF read:     " + caps.ndefRead)
println("NDEF write:    " + caps.ndefWrite)
println("Permission:    " + nfcPermissionStatus())

val text = textRecord("Hello from ScalaScript", "en", None)
val uri  = uriRecord("https://scalascript.example/nfc", None)
val mime = mimeRecord("application/octet-stream", List(1, 2, 3, 255), None)

println("Text record:   " + text.recordType + " " + text.lang + " bytes=" + text.data.length)
println("URI record:    " + uri.recordType + " bytes=" + uri.data.length)
println("MIME record:   " + mime.mediaType.getOrElse("(none)") + " bytes=" + mime.data.mkString(","))

if caps.supported && caps.ndefRead then
  val perm = requestNfcPermission()
  println("Requested:     " + perm)
  val tag = readNdef(NfcScanOptions(prompt = "Hold an NFC tag near this device"))
  println("Tag records:   " + tag.message.records.length)
else
  println("Scan skipped:  NFC hardware is unavailable on this backend")
```
