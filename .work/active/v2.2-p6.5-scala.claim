slug: v2.2-p6.5-scala
scope: literal P6.5 F2+L1 integration — a compiler in the subset that reads REAL Scala-subset SYNTAX
       (def f(x) = if x < 1 then 1 else x * f(x - 1)): lexer→token tuples, precedence-climbing parser,
       name resolution (param→local 0, func→global), lowering → EXECUTABLE CoreIR that runs to 120.
files: uniml/core/src/test/.../spike/**, specs/v2.2-*, SPRINT.md
branch: feature/v2.2-p6.5-scala
