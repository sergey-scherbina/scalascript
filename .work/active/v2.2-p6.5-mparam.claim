slug: v2.2-p6.5-mparam
scope: literal P6.5 — add MULTIPLE PARAMETERS to the compiler (last language feature): parse `def f(p0,
       p1,..) = body` (env = reversed params, arity = count → (lam N)); multi-arg calls f(a0,a1,..) ->
       (app (global f) a0 a1 ..). Completes the object language's features. def add(a,b)=a+b def main()=
       add(20, add(3, 4)) -> 27.
files: uniml/core/src/test/.../spike/**, specs/v2.2-*, SPRINT.md
branch: feature/v2.2-p6.5-mparam
