slug: register-launcher-gate
branch: feature/register-launcher-gate
agent: claude-code
started: 2026-08-10T09:26:32Z
heartbeat: 2026-08-10T09:26:32Z
status: implementing
items: register-launcher-gate
paths: file:scripts/smoke-ci.ssc
scope: |
  The one line left over: `tests/e2e/launcher-set-complete.sh` is written, green and controlled, but
  unregistered -- `scripts/smoke-ci.ssc` was held by `smoke-budget-relative-to-host`, which has now
  released. Smoke is the right home because it runs LOCALLY from the pre-push hook, and the failure
  it guards (a warm toolchain cache + a fresh worktree) is local-only: a runner's cache is always
  empty, so CI always builds and never sees it.
next: register it, run the suite, land
