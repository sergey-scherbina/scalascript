```scalascript
effect E:
  def op(): Int

def g(): Int =
  val a = E.op()
  a + 1

def f(x: Int): Int =
  if x > 0 then g() + 100 else 0

def workload(): Int =
  handle(f(5)) {
    case op(k) => k(10) + k(20)
  }

println(workload())
```
