feature/coremin-http-migrate 2026-06-22T15:30:00Z
agent: claude (mellow-shrew)
heartbeat: 2026-06-22T15:30:00Z
status: in-progress — FOUNDATION SHIPPED (makeRecord), full port DESIGNED + ready (a ~6-file core-I/O build)
DONE this turn:
  - BlockContext.makeRecord SPI + interp override (EvalRuntime, beside applyFn) -> Value.InstanceV via Opaque round-trip. Shipped 351d10bd4. BlockFormSpiTest +1 (handler replies via ctx.makeRecord; body field-accesses r.status/r.body). 4/4.
FULL-PORT DESIGN (resumable, exact):
  - Three http forms (EvalRuntime): runHttp{body} (real I/O, httpRun(None)), runHttpStub(routes){body} (stub, httpRun(Some)), httpClient(baseUrl){body} (SETS interp feature-local base url — NOT an effect handler).
  - Http.get/post/request -> Perform("Http", op, [fullUrl,...]) (StdEffectsRuntime). base url is applied INSIDE doHttpRequest via ctx.httpBaseUrl (NativeContext feature-local), NOT before the Perform.
  - PLAN: (1) add BlockContext.featureLocal(key: String): Option[Any] = None (defaulted); interp override -> nativeFeatureLocalGet. (2) http-effect-plugin: HttpBlockForm effectName="Http" under "runHttp" (no cfg) + "runHttpStub" (cfg arg = routes MapV). reply(get/post/request): stub -> route lookup -> ctx.makeRecord("Response",{status,headers,body}); real -> read HttpBaseUrl/HttpTimeoutMs/HttpMaxRetries/HttpRetryDelayMs via ctx.featureLocal + port doHttpRequest's java.net logic (self-contained; keys in NativeContextFeatureKeys/IntrinsicImpl.scala:48) -> ctx.makeRecord. (3) remove runHttp/runHttpStub from EvalRuntime(3552-3562) + EffectHandlers.httpRun + doHttpRequest + reservedApplyHeads. KEEP httpClient(baseUrl) setter in core (feature-local config, separate concern — migrate later or leave). (4) build.sbt: PluginSpec("http-effect", ...) + installBin pluginPkgs. (5) migrate any runHttp/runHttpStub interp tests -> plugin-tests (no installPlugins); verify interp + plugin-tests green.
  - makeRecord + the BlockForm SPI are proven; this is "assemble the plugin + port I/O + remove from core" — known shape, just sizeable. Do on feature branch; push to main only when fully green.
