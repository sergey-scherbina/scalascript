claim: f-object-method-varargs
branch: feature/f-object-method-varargs
agent: opus (host orchestrator — via subagent)
heartbeat: 2026-07-21T09:02:27Z
status: in-progress — add vararg-collapse for object-method calls in F (fsub.ssc).
what: BUGS f-object-method-varargs. F has NO vararg machinery; only vararg site in corpus is
      object-method Prompt.messages(msgs: Message*) (std/mcp/types.ssc) → mcp-types fails
      'arity: 1 expected, 2 given'. Fix mirrors collectObjDflts/postMeth (46626844a): new
      collectObjVarargs registry (O_m -> lead=paramCount-1, token-level trailing-* scan since
      typeText drops the star), + collapse in object-call path after parseArgL via consChain
      (add takeL; dropL/consChain exist). Gate on registered-vararg-only so non-vararg path stays
      byte-identical. Gates: fsub fixpoint, semantic (+add vararg golden), corpus 0-FAIL, dualrun.
      Does NOT edit dualrun.expected (dualrun-reconcile owns it). Plain-def/ctor varargs → BACKLOG.
