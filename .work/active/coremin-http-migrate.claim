feature/coremin-http-migrate 2026-06-22T15:55:00Z
agent: claude (mellow-shrew)
heartbeat: 2026-06-22T15:55:00Z
status: in-progress — FOUNDATION SHIPPED (makeRecord + check-iface consistency); full port DESIGNED, tractable, resumable
SHIPPED (this session): BlockContext.makeRecord SPI+interp override (351d10bd4, BlockFormSpiTest 4/4); check-with-iface threads plugin prelude (35fda6d92).
REFINED DESIGN (key simplification — NO new sbt module):
  - The existing `http-plugin` (runtime/std/http-plugin) is ALREADY a Backend (HttpInterpreterPlugin), bundled (installBin build.sbt:1097), aggregated (:869), PluginSpec (:3006), on backendInterpreter %Test (:720). Add the Http effect BlockForm THERE — no new module, no build.sbt churn.
  - httpClient(baseUrl){body} STAYS in core (a feature-local config SETTER, not an effect handler — the http-plugin authors say so, HttpIntrinsics.scala:233). Only runHttp/runHttpStub (the Http effect handlers) migrate.
PLAN:
  1. add BlockContext.featureLocal(key: String): Option[Any] = None (defaulted); interp override (EvalRuntime anon BlockContext, beside applyFn/makeRecord) -> interp.nativeFeatureLocalGet.
  2. HttpInterpreterPlugin.blockForms = Map("runHttp" -> HttpBF(None), "runHttpStub" -> HttpBF(routes-cfg)). HttpBF effectName="Http"; newHandler(ctx, args) -> args.headOption = stub routes MapV (Some) or none. reply(get/post/request): stub -> route lookup -> ctx.makeRecord("Response",{status,headers,body}); real -> read HttpBaseUrl/HttpTimeoutMs/HttpMaxRetries/HttpRetryDelayMs (NativeContextFeatureKeys, IntrinsicImpl.scala:48) via ctx.featureLocal + port EffectHandlers.doHttpRequest java.net.http logic (GET/POST/PUT/DELETE + retry loop) -> ctx.makeRecord. base-url prepend: if base.nonEmpty && !url.startsWith("http") then base+url.
  3. remove from core: EvalRuntime runHttp/runHttpStub cases (3552-3562) + EffectHandlers.httpRun + doHttpRequest + reservedApplyHeads "httpClient" stays (3016 — only remove runHttp/runHttpStub if listed). NOTE httpClient name + form stay.
  4. tests: move any runHttp/runHttpStub interp tests -> http-plugin tests OR BlockFormSpiTest; verify backendInterpreter + http-plugin + plugin-tests green. installPlugins not needed where http-plugin is on the classpath.
This is now a single-plugin edit + I/O port + core removal — tractable. Do on feature branch; push to main only when fully green.
