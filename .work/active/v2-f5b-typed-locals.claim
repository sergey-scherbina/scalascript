slug: v2-f5b-typed-locals
branch: feature/v2-f5b-typed-locals
agent: opus
started: 2026-07-27T15:10:00Z
heartbeat: 2026-07-27T17:55:00Z
status: B1 landed; now on Batch C (scljet) in the same worktree/branch
direction: Language & Compiler (F5b) + Dogfood (scljet)
worktree: /Users/sergiy/work/my/scalascript-wt-f4-arc-closure  (reused across batches — same agent, sequential)
done-so-far: |
  B1 (F5b slice 1b-3) LANDED a64109dd9 + a6a424d1a: `.length` on a String-typed local -> slen.
  Gates: X1 155 ok/0 FAIL + FIXPOINT byte-identical 406,256 B; semantic 248/248; corpus 207->205
  (-2 typed-by-design), EMPTY 0, TIMEOUT 0. Scope correction: .charAt/.substring already
  unconditional via emitPrimMeth, so the slice was `.length` only.
in-flight: |
  Batch C (scljet), NOT yet pushed — both edits live in scljet/sql.ssc:
  C2 insert-null-literal: litValue accepts the NULL keyword + parseExprAtom lowers NULL to a
     literal (was SxCol("NULL"), right answer by luck). VERIFIED: new conformance case
     scljet-insert-null PASS [INT][JS]; scljet sweep 103/104 (the 1 fail, scljet-sql-update-expr,
     is a load flake — byte-identical to expected standalone AND PASS re-run in-harness).
     scljet-address-read switched back to the literal NULL form per the bug entry; expected
     output unchanged.
  C1 ipk-moves-rowid: EditRow gains newRowid; executeUpdate ipk-normalizes BEFORE the WHERE;
     applyUpdates deletes all old rowids then inserts at the new ones (swap-safe); collision +
     non-integer target refused. Conformance case written, VERIFICATION PENDING a re-stage.
  GOTCHA (cost a full run): `bin/ssc run` reads std/scljet from the STAGED
  bin/lib/**/native-front/runtime/std/, NOT the source tree — a scljet source edit needs
  ./install.sh --dev before it can be tested at all.
