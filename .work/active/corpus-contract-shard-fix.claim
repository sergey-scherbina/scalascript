feature/corpus-contract-shard-fix 2026-07-27T09:00:00Z
agent: opus-orch
heartbeat: 2026-07-27T09:00:00Z
status: in-progress
done-so-far: FINDING — the `Corpus Contract` nightly (always-on int/js/v2 differential gate, added 2026-07-14 `48110001c`) has NEVER been green: 13 runs = 3 failure (07-15..07-17) + 10 cancelled (07-18..07-27). `cancelled` is NOT a hang — it is the `timeout-minutes: 60` wall: the latest run reached 350/485 cases at exactly 60 min. The workflow header still assumes "~415 cases … ~25 min"; the corpus is now 485 and per-case cost grew. Because `cancelled` reads as benign (not `failure`), nobody looked for 12 days — so the whole F4 front-flip landed without its main differential net (we caught those regressions by hand instead: case-object, multi-file order, md-interpolator).
next: (1) add `--shard i/N` to tests/conformance/contract.sc (round-robin over the sorted case list; the baseline compare is already subset-safe via `inScope`, and `--update-baseline` must REFUSE to run under a shard or it would truncate the file); (2) turn corpus-contract.yml into a 4-way matrix so each shard lands ~35 min inside the 60-min budget; (3) let the gate finish once and reconcile whatever verdict it produces (as of the last completed run 07-17 the drift was 2 cases: rozum-agent-schema-derived, dataset-from-generator). Also in this batch: jdk-backend-accept-teardown-race one-liner (ready in SPRINT), stale F4-flip docs in BACKLOG/SPRINT (they still say "STILL HELD" — the flip landed), orphan-worktree cleanup.
