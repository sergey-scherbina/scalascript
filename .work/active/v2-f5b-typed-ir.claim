feature/v2-f5b-typed-ir 2026-07-22T03:59:39Z
agent: opus-orch
heartbeat: 2026-07-22T12:20:09Z
status: in-progress
done-so-far: F5b (param+return-type typing, fib closed to typed i.add) + f5c bytecode lever landed through 8213fa960. f5c-1: JvmByteGen recognizes typed i.* (fixed 1.9x native-lane regression). f5c-2 KEYSTONE (8a76ea5bb): OpAnf effect-free-def registry (least-fixpoint purity; mayOp(App Global g)= !pure(g)||args.mayOp; conservative, reuses kernel effect model) -> fib no longer letified -> canParamLong unboxed path fires BY DEFAULT. fib(34) native lane ~26ms cold / ~8.5ms warm by default (was 330ms) = beats interpreter FastCode-ON. ~5x gap CLOSED. Correctness: 80-program sweep output-preserving; effectful programs byte-identical to interpreter. Gates: v2JvmBytecode/compile green, semantic 248/248, X1 fixpoint byte-identical (self-hosting unaffected - bytecode-lane-only changes).
next: f5c-3 = recognize typed f.* (double: canDouble/genDouble ~599/613) + lcell/dcell accumulator i.* (~1040/1060) - the 2 remaining __arith__-only sites - REQUIRED before f5c-4. THEN f5c-4 = apply FastCode/SelfRec removal + re-measure. ⚠ ARCH QUESTION to settle before removal: FastCode/SelfRec are on the INTERPRETER (run-ir); bytecode perf is the run --native lane. Removal is perf-neutral only if user numeric hot loops run on native lane. Need Sergiy/orch decision on the intended default execution lane for numeric code.
