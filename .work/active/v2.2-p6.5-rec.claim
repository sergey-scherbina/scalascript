slug: v2.2-p6.5-rec
scope: literal P6.5 — a compiler in the subset for a Turing-complete language (functions + RECURSION +
       local variables + global refs + control flow), reading source text and emitting EXECUTABLE CoreIR:
       compiles a recursive factorial from text → runs to 120 (two-stage self-compilation). Plus write the
       full remaining-port plan into SPRINT.md.
files: uniml/core/src/test/.../spike/**, specs/v2.2-*, SPRINT.md
branch: feature/v2.2-p6.5-rec
