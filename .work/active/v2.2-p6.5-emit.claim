slug: v2.2-p6.5-emit
scope: literal P6.5 step — a compiler F written in the subset that emits REAL EXECUTABLE CoreIR (prim
       i.add/i.mul, program/defs/entry wrapper). Two-stage self-compilation: the spike compiles F byte-
       identically to ssc1-front; running F emits CoreIR text; that text runs on run-ir to the correct
       answer. A genuine lowerer-in-the-subset emitting runnable machine code.
files: uniml/core/src/test/.../spike/**, specs/v2.2-*, SPRINT.md
branch: feature/v2.2-p6.5-emit
