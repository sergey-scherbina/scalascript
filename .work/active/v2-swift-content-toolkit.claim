feature/v2-swift-content-toolkit 2026-07-12T20:49:17Z
agent: claude-code
heartbeat: 2026-07-13T08:02:08Z
status: in-progress
done-so-far: Layer 1 (content-data plumbing) implemented and typecheck-verified against a real busi fixture. Root cause: Swift's SwiftV2Commands.emit calls FrontendBridge.convertSourceWithMetadata (v1-scalameta pipeline) exclusively; content-module extraction (contentDocument/etc) only existed in the separate self-hosted-tower pipeline (RunNativeV2/NativeV2Structural, specs/v2.1-native-content.md). Verified via a scratch probe test that busi's real offline_guide.ssc parses cleanly through the self-hosted tower (4 content modules extracted correctly, root+3 transitive imports). Implemented: SwiftV2Commands.contentModulesBase64For (a second, non-fatal RunNativeV2.compile call, extracts .contentModules, encodes via existing NativeContentCodec, base64s it); SwiftBackend.generate now always emits Sources/AppCore/ContentModules.swift (empty base64 when no content); SwiftRuntime.scala sourcePart3 (new split, ~350 lines) ports ContentNativePlugin.scala's data accessors (contentDocument/contentSection/contentBlock/contentData/contentMetadata/contentPlainText/contentToMarkdown/contentModules/contentModule/contentModuleSection/contentModuleBlock/contentModuleData/contentModuleMetadata, all 13 registered in coreBuiltinGlobals) plus a from-scratch Swift binary decoder for NativeContentCodec's documented format. Verified: emit-swift on a real fixture importing contentDocument()/contentPlainText() against busi's offline_guide.ssc produces a real ~127KB embedded base64 blob; swiftc -typecheck on the whole generated AppCore package is clean (exit 0). Full `bin/ssc-tools package --target macos` build in progress (not yet confirmed to actually RUN correctly at runtime).
next: confirm the full build+run works end to end (real crash-free execution, not just typecheck) against the toolkit-stage1-check.ssc fixture (contentDocument+contentPlainText only, no toolkit rendering yet). Then verify against busi's actual offlineGuidePlain()/offlineGuideMatrixPlain() (contentPlainText(contentSection/contentBlock)) as the real-world proof. Commit Layer 1 once verified. Layer 2 (contentToolkitNode/Block/Section rendering, ~520-line port of ContentToolkitJs.scala) is NOT started — separate, deferred slice per the room's own scoping discussion.
