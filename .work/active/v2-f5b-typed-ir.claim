feature/v2-f5b-typed-ir 2026-07-22T03:59:39Z
agent: opus-orch
heartbeat: 2026-07-22T13:18:36Z
status: in-progress
done-so-far: v2-f5c Option-A prereqs (through a2f6b31c9). SLICE 1 DONE (48e31b163): link-time bytecode->interpreter fallback in RunNativeV2.runBytecode (try emitProgram catch Unsupported|ASM MethodTooLargeException|ClassTooLargeException => runVm; pre-execution, side-effect-safe; RUNTIME failures NOT caught). scljet-hello/-jdbc via --bytecode now fall back byte-identical, no crash; gap #1 (Method-too-large) closed. Gates: semantic 248/248, no regression on working programs, CLI compiles. SLICE 2 (stack-safe effectful loops) ANALYZED not landed (spec §8): root cause = emitChain/emitLetChain give each chained Seq/Let statement a FRESH Ctx dropping localTailTargets/localFrameArity, so the effectful-loop recur fails the Bounce guard -> Emit.app recursion -> overflow at ~500k. Fix = 2 coupled pieces (propagate tail-context to chained Ctx w/ de-Bruijn key-shifting + unroll the effectAwareWhile initial call), high regression surface (whole native tier), a dedicated gated increment NOT a bounded slice.
next: prereq #2 (SLICE 2) is the remaining blocker to a safe bytecode default. Dedicated JvmByteGen increment: propagate localTailTargets/localFrameArity/selfGlobal (NOT startLabel) into emitChain/emitLetChain Ctx with key-shifting per chain extension, + make effectAwareWhile initial App(Local 0) unroll the Bounce. Gate: pattern-match-heavy >=500k on --bytecode no overflow byte-identical to interp, semantic 248/248, fixpoint byte-identical, full examples sweep byte-identical, v2JvmBytecode/compile. THEN switch default (with fallback) + f5c-3 + FastCode/SelfRec removal with flip-level rigor. f5c-3/f5c-4 stay BLOCKED on #2.
