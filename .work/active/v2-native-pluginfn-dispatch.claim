feature/v2-native-pluginfn-dispatch 2026-07-10
agent: claude (lucky-perch)
status: in-progress
scope: VM-side (Runtime.scala) fix — native front's App(Global(f),args) where f is a
  registered Prim OP-HANDLER (V2PluginRegistry.handlers) compiles to Prim(f,args), the SAME
  form the bridge emits (identical, safe — effect-runners are block-form, not handlers, so
  untouched). Unblocks the pure plugin-fn UNBOUND cluster (aesGenKey/verifyEd25519/spark
  schema/parseYaml…). Runtime.scala ONLY — NOT ssc1-front/lower (K62's active files).
  Coordinated: offered K62 the division rozum /79.
next: locate App(Global) compile site, add handler-fallback, test aesGenKey + conformance
