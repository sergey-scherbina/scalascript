-- Backend driver: ssc0 SOURCE -> JS. Reuses the shared codegen (lib/backend-js-gen.ssc0) and only
-- adds the ssc0 front (ssc0c). toJS = parse ssc0 -> IrProg Data -> genProg.
import "ssc0c.ssc0"
import "backend-js-gen.ssc0"

def toJS = (src) => genProg(compileModule(parseTop(lex(src))))
