feature/corpus-contract-shard-fix 2026-07-27T09:00:00Z
agent: opus-orch
heartbeat: 2026-07-27T15:51:44Z
status: gate FIXED + it already paid off — first finishable run exposed a real regression on the DEFAULT exec lane (bytecode), fixed in c3841d01e
done-so-far: FINDING — the `Corpus Contract` nightly (always-on int/js/v2 differential gate, added 2026-07-14 `48110001c`) has NEVER been green: 13 runs = 3 failure (07-15..07-17) + 10 cancelled (07-18..07-27). `cancelled` is NOT a hang — it is the `timeout-minutes: 60` wall: the latest run reached 350/485 cases at exactly 60 min. The workflow header still assumes "~415 cases … ~25 min"; the corpus is now 485 and per-case cost grew. Because `cancelled` reads as benign (not `failure`), nobody looked for 12 days — so the whole F4 front-flip landed without its main differential net (we caught those regressions by hand instead: case-object, multi-file order, md-interpolator).
next: full 4-shard verdict from run 30281019432 (on e8214a277, i.e. WITHOUT the OpAnf fix — so it will still show head-field-effect-shadow v2 FAIL). Triage every entry: regressions fixed or filed, closed gaps recorded via an UNSHARDED --update-baseline. Do not update-baseline a regression away.
