# ScalaScript 2.1 — toolchain independence

Status: **in progress** (2026-07-10)

This specification defines the release-wide ScalaScript 2.1 production-path
contract. It complements the feature-specific `v2.1.x` Distributed Streams
documents; those version labels remain valid and do not define the toolchain
packaging contract.

## 1. Purpose

The standard JVM path must execute and package ordinary `.ssc` programs without
Scalameta, Scala CLI, the Scala compiler, javac, or the `java.compiler` module:

```text
.ssc source
  -> native Markdown/code frontend
  -> native type checker
  -> CoreIR
  -> v2 VM or direct ASM JVM bytecode
  -> result / deterministic JAR
```

Scala remains the implementation language of the host components in 2.1. A
prebuilt distribution may therefore contain the Scala runtime and ASM. Building
ScalaScript itself from source still requires a Scala compiler. This milestone is
about the installed compiler/runtime path, not a source-bootstrap rewrite.

## 2. Dependency tiers

The distribution has two explicit tiers. A standard command must never discover
and silently enter the tools tier.

| Tier | Required contents | Forbidden dependencies | Intended commands |
|---|---|---|---|
| **standard** | staged launcher, native frontend tower, native checker/lowerer, CoreIR VM, direct ASM emitter, scalameta-free plugin runtime, selected runtime plugins, Scala runtime, ASM | Scalameta family, Scala CLI, `scala3-compiler`, compiler driver, `javax.tools`, `jdk.compiler`/`java.compiler` | default `ssc run`, `ssc run --bytecode`, `ssc build-jvm` |
| **tools / compatibility** | everything needed by an explicitly selected legacy or platform compiler route | none beyond the normal security/reproducibility policy | `--v1`, `--compat-frontend`, Scala/Java fences, legacy `compile-jvm`, Spark, Scala.js/WASM source compilation |

Normative rules:

1. `PATH` lookup for `scala-cli`, `scalac`, or `javac` is forbidden in the
   standard tier.
2. Loading a Scalameta or compiler class reflectively is equivalent to a direct
   dependency and is forbidden in the standard tier.
3. A tools-tier feature requested from a slim installation fails before partial
   execution with a diagnostic naming the missing tier and installation remedy.
4. Standard execution must remain functional after physically deleting all
   Scalameta-family JARs and the installed compiler directory.
5. The standard launcher's reachable module graph must not require
   `java.compiler` or `jdk.compiler`.

Physical staged layout after TI-8.3:

```text
bin/ssc                           # default standard entry
bin/ssc-standard                  # equivalent descriptive alias
bin/lib/standard/ssc.jar          # class-filtered native entry only
bin/lib/standard/jars/            # explicit standard dependency allowlist
bin/lib/standard/native-front/    # checker/lowerer tower + std sources

bin/ssc-tools                      # explicit full compatibility entry
bin/lib/ssc.jar + bin/lib/jars/    # compatibility runtime
bin/lib/compiler/                  # optional compiler/plugins
```

TI-7.1 landed this standard layout in `c43d23e59`. TI-7.2 `be229a70d` added the
explicit tools launcher to `installBin`, `install.sh`, and self-install. The
standard entry initially delegated explicitly selected `run --v1` and
`--compat-frontend`; other tools-backed surfaces returned the bounded
tier/remedy diagnostic. TI-7.3 `65773c2fe` made the separation a CI release gate: it copies
the staged distribution, deletes the compatibility JAR/plugin/compiler trees,
the full CLI JAR, and both compatibility launchers, then executes the complete
representative standard surface from the surviving files. The TI-8.1 audit
`e4cd55b36` then made every dependency JAR a static-scan root and removed H2's
optional `SourceCompiler*` family from only the standard-tier copy; the original
driver remains intact in tools. TI-8.3 `e28560761` then made `ssc` and
`ssc-standard` identical standard entries and removed StandardMain's tools
subprocess delegation. Scalameta/v1/compiler surfaces now require an explicit
`ssc-tools` invocation; both contributor and self-install paths preserve that
three-launcher contract.

## 3. Architecture and ownership

### 3.1 Standard path

The native frontend is the `mira-md -> ssc1-front -> ssc1-check -> ssc1-lower`
tower. The installed launcher runs the tower through a prebuilt kernel JAR and
staged `.ssc0`/runtime resources; it does not invoke Scala CLI over `v2/src`.

`ssc1-check` is **mandatory** before the native frontend becomes the default.
Dropping Scalameta while also bypassing type checking would replace a dependency
but regress the language contract. The checker may deliberately use `Dyn` at
documented plugin/FFI boundaries, but it must:

- accept every valid deterministic default-lane corpus program;
- reject the negative type-conformance corpus;
- produce source-located diagnostics;
- never emit runnable CoreIR containing the parser's `_err` sentinel.

Imports are resolved before type checking and lowering. Resolution is DFS,
canonical-path deduplicated, relative to the importing module first, and then to
the staged standard-library root. Multiple command-line source files participate
in one module closure. Program arguments after `--` are data, never source paths.

### 3.2 Compatibility frontend

The current v1-parser/Typer `FrontendBridge` remains an optional migration and
rollback component. Its AST and `BlockForm` adapters belong in the tools tier and
must not be referenced by standard runtime classes. It may be removed after one
release with a green native corpus and no unique compatibility cases.

### 3.3 Plugin runtime boundary

The standard v2 runtime owns a Scalameta-free value and intrinsic SPI. Runtime
values, callable values, effects, records, collections, errors, and native
contexts must be expressible without `scala.meta.Tree` or a v1 frontend AST.

Existing v1 `NativeImpl` plugins may be supported by an adapter in the tools tier.
That adapter is not allowed on the classpath of standard plugin tests. New plugin
work continues to live under `runtime/std/`; this specification changes the value
boundary, not the plugin ownership rule.

#### 3.3.1 Native provider interface

The standard JVM tier uses a dedicated ServiceLoader contract under
`ssc.plugin`, built without `core`, `backendInterpreter`, `pluginApi`, or any
Scalameta/compiler dependency:

```scala
trait NativePlugin:
  def id: String
  def install(context: NativePluginContext): Unit

trait NativePluginContext:
  def argv: List[String]
  def databases: Map[String, NativeDatabaseConfig]
  def contentModules: List[NativeContentModule]
  def invoke(fn: ssc.Value, args: List[ssc.Value]): ssc.Value
  def withEffect(effectTag: String)(handler: (String, List[ssc.Value]) => ssc.Value)(body: => ssc.Value): ssc.Value
  def register(name: String)(fn: List[ssc.Value] => ssc.Value): Unit
  def registerGlobal(name: String, arity: Int)(fn: List[ssc.Value] => ssc.Value): Unit
  def registerValue(name: String, value: ssc.Value): Unit
  def registerFields(tag: String, fields: Vector[String]): Unit
```

`ssc.Value` is the 2.1 standard runtime value boundary. Closures and opaque host
handles already have first-class `ClosV`/`ForeignV` representations, so a second
v1-shaped value hierarchy is not introduced. `NativePluginContext` is the only
registry mutation surface exposed to providers; providers do not import
`V2PluginRegistry` directly. Providers also invoke user callbacks only through
`context.invoke`; this keeps trampoline/closure-environment details inside the
standard host and gives server, streaming, SQL, and UI providers one callback
contract.

Normative loading rules:

1. Providers register through
   `META-INF/services/ssc.plugin.NativePlugin` and are installed in stable
   `id` order.
2. Duplicate intrinsic/global ownership is an error naming both providers;
   last-writer-wins registration is forbidden in the standard tier.
3. `ssc run --native` loads only this provider set. It must not call or link
   `ssc.bridge.PluginBridge`; `--compat-frontend` continues to load the v1
   `NativeImpl` adapter from the tools tier.
4. Feature providers live under `v2/runtime/std/<feature>-plugin/` and may
   depend on a standalone feature runtime (for example the JDBC executor), but
   not on v1 frontend/interpreter/plugin-api modules.
5. The initial migration order is crypto/fs/os/json, followed by HTTP, SQL, and
   UI. A feature switches only after its native provider has an assembled
   VM/ASM smoke and a classpath-reference gate.
6. Runtime globals such as `args`, `cwd`, `sep`, and `platform` are installed
   by a small standard host provider, not by the compatibility bridge.
7. `invoke` accepts a callable v2 runtime value and explicit arguments, checks
   arity through the normal runtime path, and returns the callback's final
   value. A non-callable value is a bounded provider error, not an unchecked
   cast or compatibility fallback.

#### 3.3.2 HTTP migration boundary

HTTP migrates in two independently shippable provider slices:

1. The standard client/value slice owns outbound `httpGet`/`httpPost`/
   `httpPut`/`httpPatch`/`httpDelete`, response builders, cache helpers, timeout,
   and retry state. It uses the JDK HTTP client, represents `Response` as the
   ordinary v2 data constructor `(status, headers, body)`, and reuses the native
   JSON codec for non-string `Response.json` bodies. A deterministic loopback
   unit test plus assembled VM/ASM response-builder smoke are mandatory.
2. The server slice owns `route`, `serve`/`serveAsync`, `stop`, middleware,
   streaming responses, SSE, uploads, and WebSockets. These operations require
   an explicit native server-host SPI for route registration, callback
   invocation, and lifecycle. Any operation whose host hook has not landed must
   fail with a bounded "native HTTP server unavailable" diagnostic; calling the
   v1 interpreter/server bridge or silently treating registration as success is
   forbidden.

The client/value slice may land before the server slice. It does not by itself
complete HTTP migration or TI-5.

#### 3.3.3 SQL migration boundary

The standard launcher receives each explicit root through the frozen structural
`NativeCompilation/4` product: checked CoreIR, self-hosted Frontmatter YAML,
self-hosted content modules for the complete linked closure, and source
identities cross the seed boundary as ordinary `ssc.Value` ADTs. The content
module product retains normalized direct-import edges, namespace, and the
canonical `DocumentContent` projection; its detailed contract is normative in
[`v2.1-native-content.md`](v2.1-native-content.md). The Scala seed
validates/maps those values but does not parse YAML, Markdown, content attrs, or
frontend-emitted text. It exposes immutable `NativeDatabaseConfig` values
through `NativePluginContext.databases` and content values through
`NativePluginContext.contentModules`; the
retired `NativeFrontmatter`/`SimpleYaml` host path is absent from the standard
artifact and runtime class-load trace. An entry preserves `url`, optional
`user`, `password`, and `driver` strings so `${env:...}` and other JDBC-runtime
substitutions remain lazy. Identical duplicate names across roots are accepted;
conflicting definitions fail before opening a connection or executing user
code.

The native SQL provider lives under `v2/runtime/std/sql-plugin/` and may depend
on the standalone `backendSqlRuntime`. It converts the native configs into one
lazy `ConnectionRegistry` and owns the standard `DriverManager.getConnection`,
`Db.query`, and `Db.execute` surfaces. Queries return ordinary v2 lists of maps
with JDBC column labels as keys; update/DDL statements return `Int`. Bind values
are converted explicitly between `ssc.Value` and the executor rather than
passing v1 `PluginValue` or interpreter values through `ForeignV`.

The first independently shippable SQL slice covers named H2 front-matter,
parameter binding, DDL/DML, and row reads on both native VM and direct ASM. It
must include malformed/conflicting-config tests and the standard static/runtime
classpath gates. Typed `Db.insert`/`Db.update`, PostgreSQL LISTEN/NOTIFY, and
native lowering of fenced `sql`/`transaction` blocks are follow-up SQL slices;
they may not silently enter `PluginBridge` while pending.

#### 3.3.4 UI migration boundary

The standard UI provider must not reuse `frontendCore` runtime values because
that module currently depends on the Scalameta-coupled v1 `core`. The native
provider instead represents signals, event descriptors, and view nodes directly
on `ssc.Value`/`ForeignV` with named-method access. Mutable and derived signals
must expose the normal callable/get/set/update behavior, and user callbacks run
only through `NativePluginContext.invoke`.

The first UI slice owns `signal`, `computedSignal`, `eqSignal`, `textNode`,
`signalText`, `showSignal`, `fragment`, `element`, basic signal event builders,
and `emit`. `emit` performs deterministic UTF-8 static rendering to
`<outDir>/index.html`, escapes text and attributes, resolves current signal
values, and never invokes a frontend compiler. Unit tests plus assembled VM/ASM
must compare the emitted bytes, not merely accept a `ForeignV` or `Stub` result.

The standard JVM/static lane also constructs core-free declarative
`fetchUrlSignal` and `fetchAction` values so imported JSON/fetch helpers can be
checked and evaluated consistently. A fetch signal reads as empty until a
target renderer executes its descriptor; network execution remains the emitted
browser runtime's responsibility. Framework-specific SPA generation,
`serve(view, port)`, keyed reconciliation, data-table actions,
storage/WebAuthn, and desktop/mobile renderers remain follow-up UI slices. Until
their native host contracts land, they must fail with bounded native diagnostics
or unresolved-provider errors; loading the v1 frontend plugin/`frontendCore` as
a fallback is forbidden.

#### 3.3.5 Effect migration boundary

The v2 kernel already owns a core-free dynamically scoped effect-handler stack.
Providers access it only through `NativePluginContext.withEffect`: the host
pushes the named handler, evaluates the by-name body, and pops it in `finally`.
Providers never manipulate `V2EffectContext`, interpreter `Computation`, v1
`BlockForm`, `EffectHandler`, or `SpiValue` directly. User thunks and operation
callbacks continue to run through `context.invoke`.

The representative first provider owns `State` plus the curried
`runState(initial) { body }` runner. `State.get`, `State.set`, and
`State.modify` use the innermost active state; `modify` invokes its callback
through the host; the runner returns `(finalState, bodyResult)`. Nested runners
must restore the outer handler, and thrown callbacks must not leak a handler
into the next run. Unit tests and assembled VM/ASM cover both mutation and
nested restoration.

Logger formatting/collection, Random, Clock, Env, Retry, Cache, Async, and
stream handlers are follow-up provider slices. Until migrated, their native
runner globals may not be populated by the v1 block-form bridge.

### 3.4 VM and ASM backends

Both standard backends consume the same checked CoreIR:

- the VM compiles CoreIR to closures and executes it;
- `v2JvmBytecode` emits JVM bytecode directly with ASM.

The old v1 `AsmJitBackend` is not the 2.1 artifact backend: it consumes the
Scalameta-coupled v1 runtime graph. The v1 `JavacJitBackend` and that legacy ASM
JIT remain tools-tier compatibility implementations only.

## 4. CLI and artifact contract

### 4.1 Execution

Explicit standard and tools routes:

```text
ssc run --native file.ssc [more.ssc ...] -- [program args ...]
ssc run --native --bytecode file.ssc [more.ssc ...] -- [program args ...]
ssc-tools run --compat-frontend file.ssc # tools tier: Scalameta bridge
ssc-tools run --v1 file.ssc              # tools tier: complete v1 rollback
```

At cutover:

- plain `ssc run` is identical to `ssc run --native` and uses the v2 VM;
- `--bytecode` selects direct CoreIR-to-ASM execution without changing frontend
  or program semantics;
- `--native` remains an accepted, idempotent assertion for CI and deployment;
- `--v2` remains a deprecated alias for the default v2 runtime and no longer
  implies the Scalameta bridge;
- the old bridge is selected only by explicit `ssc-tools ...
  --compat-frontend`; the standard launcher never spawns tools.

The selected route is observable with `ssc info --execution-plan ...` (or an
equivalent stable machine-readable command). Tests must not infer the route from
timing or classpath accidents.

### 4.2 JVM artifacts

The new direct-ASM artifact command is intentionally distinct from the existing
Scala-source-backed `compile-jvm [--bytecode]` path:

```text
ssc build-jvm file.ssc [more.ssc ...] -o app.jar
```

`build-jvm` must:

- run the native frontend and native checker;
- emit `.class` files directly through `v2JvmBytecode`;
- include a deterministic manifest/main entry, required v2 runtime metadata,
  linked module definitions, and selected standard plugins;
- preserve `.ssc` source/line mapping in JVM diagnostics;
- produce byte-identical JARs for identical inputs, options, and dependency set;
- run through `java -jar app.jar -- ...` without Scala CLI, scalac, or javac.

The artifact is a deterministic, self-contained JAR rather than a launcher
that searches the installation at runtime:

1. `ssc/gen/Entry.class` is emitted directly by `v2JvmBytecode` and owns the
   JVM `main(String[])` method. The method initializes the standard artifact
   runtime, installs compiled definitions, evaluates the entry, and applies the
   same unresolved-effect/result validation as `ssc run --native --bytecode`.
2. Runtime classes are merged from an explicit standard-tier allowlist: v2
   core, native plugin SPI/providers, Scala runtime, and provider-specific leaf
   dependencies. The merge rejects conflicting duplicate entries, combines
   `META-INF/services/ssc.plugin.NativePlugin` deterministically, and excludes
   signatures, v1 frontend/interpreter/bridge, Scalameta, and compiler families.
3. `META-INF/scalascript/artifact.properties` records the artifact format,
   sorted source identities and SHA-256 digests, selected providers, and native
   database configuration. It contains no timestamps or absolute paths.
4. Every ZIP entry uses fixed metadata and lexical ordering. Manifest bytes,
   provider order, dependency order, and stored metadata are independent of the
   checkout path and wall clock.
5. Imported modules and multiple explicit roots are lowered into one linked
   `Program`; duplicate/conflicting module definitions follow the same native
   frontend rules as direct execution.
6. Generated methods carry `SourceFile`, line tables, and a
   `SourceDebugExtension` SMAP whose file table contains every explicit root.
   At minimum, entry statements and named definitions map to their originating
   `.ssc` line; generated helper frames may map to the owning statement/def.

TI-6 is delivered in independently green slices:

- [x] executable generated entry + deterministic standard fat-JAR writer
  (2026-07-10, `a8a86fffe`), including hello/argv/native-plugin `java -jar`
  smoke. `ssc.gen.Entry.main` delegates only to the core-free artifact runtime;
  the writer merges a strict runtime/provider allowlist, combines ServiceLoader
  descriptors, rejects conflicting entries, writes stored entries in lexical
  order at the ZIP epoch, and runs the native checker before ASM. Two staged
  argv+crypto builds were byte-identical (26,291,502 bytes; SHA-256
  `95590553b0174f3026a947fbb48f000a3cf878cf4e61c114d928c86a33b2d746`),
  executed with `java -jar` while compiler commands were absent, and passed
  entry/reference plus `jdeps` forbidden-dependency gates;
- [x] import/multi-root linking, embedded runtime configuration, and duplicate
  resource/definition diagnostics (2026-07-10, `147531fa7`). The native module
  closure is packaged as one checked `Program`; source hashes include explicit
  roots plus resolved imports; database declarations are serialized and rebuilt
  before ServiceLoader installation. SQL artifacts add only the required native
  provider/runtime/driver allowlist, omit H2's optional Java source compiler,
  and run H2 DDL/DML/query through `java -jar` with no installation lookup or
  `java.compiler` edge;
- [x] `.ssc` SourceFile/line/SMAP propagation with a failing stack-trace test
  (2026-07-10, `e4f16baaf`). The core-free emitter accepts explicit source
  coordinates and emits `SourceFile`, `LineNumberTable`, and JSR-45
  `SourceDebugExtension`; the file table lists explicit roots first and the
  linked import closure after them. A lexical, Scalameta-free host resolver and
  fenced-source scanner recover declaration/entry starts without absolute
  paths. A deliberate `jsonParse` failure reports
  `source-map-failure.ssc:4`;
- [x] two-clean-build byte identity plus no-compiler/no-Scalameta classpath and
  `jdeps` release gates (2026-07-10, `a8e6742fa`). The CI gate copies identical
  roots into two unrelated clean directories, compares the complete JAR bytes,
  hides `scala-cli`/`scalac`/`javac`, executes hello/import/argv/crypto/SQL, and
  scans entries, constant-pool output, class dependencies, and module graphs.
  It writes a stable TSV baseline for release review.

Legacy `compile-jvm`, `.scjvm`, Scala facades, and Scala-source passthrough remain
tools-tier compatibility surfaces until separately migrated. The word
`--bytecode` on those commands does not make them part of the native ASM path.

## 5. Compatibility policy

Every corpus row is classified as exactly one of:

1. **standard deterministic** — VM and ASM observable output must match;
2. **standard server/long-running** — readiness and bounded interaction are
   tested instead of process completion;
3. **standard nondeterministic** — normalized semantic assertions replace byte
   comparison;
4. **backend-specific tools tier** — Scala/Java fences, Spark, Scala.js/WASM, or
   another explicitly compiler-backed target;
5. **document/non-code** — no executable ScalaScript block;
6. **temporarily unsupported compatibility case** — named in the baseline with
   a stable diagnostic and owner; no silent `_err`, empty output, or success.

An increase in category 4–6 requires a spec change. A parser/lowerer exit code of
zero is not success when the emitted IR contains `_err` or unresolved parser
sentinels.

## 6. Portable gates

All gates resolve the repository from their own script path or Git worktree;
absolute developer paths are forbidden. Each gate writes a stable JSON or TSV
summary in addition to the human report.

Required gates:

- **native-front corpus:** extract/import/parse/check/lower/run classification for
  every `examples/*.ssc` input, including sentinel detection;
- **VM/ASM parity:** checked CoreIR executed by both standard backends, with the
  classifications in §5;
- **artifact reproducibility:** two clean `build-jvm` runs compared byte-for-byte
  and executed with `java -jar`;
- **slim-filesystem negative gate:** delete compiler and Scalameta JARs, hide
  Scala CLI/scalac/javac from `PATH`, then run hello/import/argv/plugin/JAR smokes;
- **JRE-module negative gate:** run the standard launcher/runtime with
  `java.compiler` and `jdk.compiler` unavailable and inspect it with `jdeps`;
- **classpath gate:** fail when a standard-tier JAR contains or references
  `scala.meta`, v1 frontend AST, compiler-driver, or `javax.tools` symbols.

## 7. Audited baseline (2026-07-10)

The historical audit was recorded through `1cc748f5c`, with native module-loading
work through `4815915ba`. TI-2 replaced its ad-hoc loops with the portable gates
landed in `7ba4d413b` and lane classification in `4d538ae98`; the plugin temp-tree
leak found by the sweep was fixed in `784ac95d3` before the authoritative rerun.

| Surface | Current state | 2.1 requirement |
|---|---|---|
| Direct v2 ASM emitter | CoreIR-to-ASM exists; execution is in-memory and reached from the Scalameta bridge in the public CLI | native checked CoreIR input plus deterministic JAR output |
| Native import/parse/lower | The current import-aware TI-8.2 route covers all 195 files: 194 OK, 0 host errors/timeouts, 1 non-code, with 68 detectable `_err` sentinel rows, all explicitly classified outside the standard parser queue. | no sentinel-bearing success; every row classified |
| Native checker | The current direct full-file checker result is 194 OK, 0 `TYPEERR`, 1 non-code, with no checker crash/timeout. | mandatory, corpus-valid, negative-type green |
| Native plugin-enabled run | 44 OK, 82 runtime errors, 68 not run because of classified sentinels, 0 frontend host errors/timeouts, 1 non-code | all standard rows green/classified on VM and ASM |
| Plugin boundary | `169fa2c28` split `RunNativeV2` onto the deterministic core-free `ssc.plugin` ServiceLoader host and migrated process globals plus nine crypto helpers; subsequent slices added FS/OS, JSON, HTTP, SQL, UI, State, Storage, reactive signals, and the `2da4183f5` YAML provider. The compatibility route still uses `PluginBridge`; advanced HTTP/SQL/UI and remaining effect providers remain. | standard runtime/plugin graph has no v1 frontend or Scalameta edge |
| Public ASM run | `ssc run --bytecode` uses the v1 Scalameta frontend bridge | native frontend/checker by default |
| JVM artifact path | `build-jvm` writes deterministic self-contained direct-ASM JARs for linked roots/imports, native providers, SQL configuration, and `.ssc` SourceFile/line/SMAP diagnostics | release-gated direct-ASM `build-jvm` path |
| Installation | TI-7 stages separate standard/tools trees; the copied standard tier remains executable after the full CLI, tools launcher, compiler directory, legacy native-front, and compatibility JAR tree are deleted | make the standard launcher the default in TI-8 |
| Java compiler | the v1 default JIT remains javac-backed in tools; TI-8.1 filters H2's optional source compiler from standard and runs the whole tier with a `jdeps`-derived `--limit-modules` set where both compiler modules are unresolvable | make native standard execution the default launcher after TI-4 parity |
| Bridge VM/ASM corpus | 95 byte-identical, 12 both-fail, 45 backend/target/frontend-specific, 7 nondeterministic, 36 server; zero unexplained mismatch or one-sided error after classification | native checked CoreIR; standard deterministic rows remain byte-identical |
| Standard native VM/ASM corpus | Current TI-8.2 sweep: 31 byte-identical, 35 honest both-fail, 129 skipped server/backend/nondeterministic, with 0 stdout mismatch and 0 one-sided error | eliminate/classify both-fail standard rows before default cutover |
| Portable gates | `scripts/bc-parity-sweep` and `scripts/native-front-corpus` resolve any worktree, use a portable timeout wrapper, and write one TSV row per input | keep as CI/release gates and tighten categories as slices close |

This baseline is deliberately stricter than the earlier claim that the backend is
"almost ready": ASM emission is advanced, but the public frontend, plugin value
graph, artifact writer, packaging, and negative gates still define substantial
release work.

## 8. Implementation slices and behavior checks

- [x] **TI-1 Contract and plan (2026-07-10):** this spec is linked from `SPEC.md`; all slices
  are queued in `SPRINT.md` before implementation.
- [x] **TI-2 Portable baselines (2026-07-10, `7ba4d413b` + `4d538ae98`):**
  native-front and VM/ASM corpus gates run from any worktree, expose `_err`
  separately from frontend success, classify all 195 inputs, and emit TSV.
- [x] **TI-3 Production native entry (2026-07-10, `0ccecb44d`):** staged
  `ssc run --native` handles hello, normalized relative/std imports, multiple
  sources, argv, explicit sentinel diagnostics, VM/direct ASM, and plugin
  intrinsics with Scala CLI absent.
- [x] **TI-4 Frontend parity:** all corpus rows are classified, standard rows
  parse/check/lower without sentinels, and the mandatory checker passes both
  positive and negative suites.
  Final release result: 194/194 code rows parse and check, the remaining one is
  non-code, sentinel taxonomy has zero standard gaps, VM/ASM has zero mismatch
  or one-sided failures, and the standard runtime blocker count is zero.
  - [x] **Numeric/string checker inference (2026-07-10, `66b7c4ede`):** infer
    Float literals and mixed numeric arithmetic, String repetition, and
    substitution-aware String concatenation while retaining negative numeric,
    repetition-count, boolean, and condition checks. Direct corpus result:
    189 OK / 5 `TYPEERR` / 1 non-code after the companion prefix fix below;
    every remaining reject is a frontend parser-sentinel row.
  - [x] **Prefix/postfix precedence (2026-07-10, `6e8464ea8`):** postfix
    selection/application now binds inside unary `!`, `-`, and `~`. The
    sentinel-clear `!exists(path)` checker rejection is gone, and assembled
    native VM/direct-ASM tests cover all three operators on call operands.
  - [x] **Ordered immutable initialization (2026-07-10, `5db137a20`):** physical
    newlines now infer statement separators at top level and directly inside
    explicit braces when both adjacent tokens can terminate/start statements.
    Top-level immutable values and tuple bindings use empty global cells whose
    initializers run once from the entry in document order. This prevents a
    plugin-backed value from running before earlier effects while keeping the
    value visible to global definitions. Faithful assembled regressions retain
    `val rows = Db.query(...)` after H2 DDL/DML and a nested `val inside =
    runState(...)`; VM and direct ASM produce identical `1/7/Ada/true` and
    `17/20/2/101/101/2` output. The full native-front corpus completes 195 rows
    without crash/timeout (192 front success, 190 checker success), and the
    `v2-*` conformance slice remains 8/8.
  - [x] **Unresolved-result parity (2026-07-10, `7192cd6e4`):** shared VM/ASM
    result handling rejects top-level missing-dispatch `Stub` values and dotted
    unhandled runtime-effect `Op` values. The historical
    `Wallets.metaMask` bytecode lane now exits nonzero instead of printing the
    free effect as a successful result; undotted user-level free-monad `Op`
    data remains representable.
  - [x] **Braced interpolation expressions (2026-07-10, `86a2de03a`):** the
    self-hosted lexer balances `${...}`, nested braces, and quoted literals,
    then parses the complete inner expression instead of treating selector/call
    suffixes as outer string text. `index.ssc` and a focused `mkString(", ")`
    fixture now produce correct byte-identical output on native VM/direct ASM.
  - [x] **Local direct-ASM tail recursion (2026-07-10, `3153fb2db`):** compiled
    `LetRec` groups map environment-relative De Bruijn peer references to their
    generated methods. Tail calls return a `Bounce` over a frame rebuilt as
    `captured ++ tied-group ++ target-args`, so local self and mutual recursion
    remain stack-safe without invoking the VM backend. The full 13-row
    `recursion.ssc` output matches VM, in-memory ASM, and `build-jvm` under
    `-Xss256k`.
  - [x] **Multiline function types and UI fetch parity (2026-07-10,
    `d6b9ae9ce`):** type skipping balances nested function-type parentheses, so
    a `() => String` parameter cannot close its containing multiline parameter
    list. Direct ASM now enforces the VM's closure arity, and the core-free UI
    provider constructs declarative fetch signals/actions. The real
    `ui-fetch-json.ssc` output is byte-identical on standard VM/direct ASM.
  - [x] **Host-safe multiline lambdas and tuple patterns (2026-07-10,
    `d4513cb8a`, source diagnostic `ac441ef62`):** `=>` opens an offside block,
    3+-element tuple patterns use the expression runtime's right-nested `Pair`
    shape, and the native launcher names every input whose remaining parse
    sentinel blocks execution. `dsl-mini-language.ssc` no longer crashes on
    `Pair/2`; `graph-fullstack-rdf.ssc` imports the current `std/ui/data.ssc`
    table API instead of a deleted module. The full frontend sweep has zero
    host errors/timeouts.
- [x] **TI-5 Plugin boundary (2026-07-10):** representative JSON, YAML, HTTP, SQL, UI, and effect
  programs run without Scalameta/v1-frontend JARs.
  - [x] **Native SPI + crypto pilot (2026-07-10, `169fa2c28`):** added the deterministic `ssc.plugin`
    ServiceLoader host, a core-free crypto provider, duplicate-ownership tests,
    and switch `--native` VM/ASM plugin loading away from `PluginBridge` while
    preserving the staged `std/crypto.ssc` smoke. `RunNativeV2` is physically
    separate from the compatibility runner; static `jdeps`/`javap` gates and a
    runtime class-load trace reject bridge/Scalameta edges.
  - [x] **Native FS/OS providers (2026-07-10, `7335d2a1c`):** migrated the
    complete JVM `std.fs` surface and the environment/path/temp portion of
    `std.os` onto core-free providers. Host-owned `args`, `cwd`, `sep`, and
    `platform` remain single-owner globals. Unit tests cover text/byte and
    file-tree/path behavior; the assembled VM/ASM smoke performs the same
    write/append/read/delete round-trip with Scala CLI absent, while JAR `jdeps`
    and runtime class-load checks reject v1/Scalameta edges.
  - [x] **Native JSON provider (2026-07-10, `9798dfc5c`):** migrated tolerant
    `jsonValue`, `jsonStringify`, strict `jsonParse`/`jsonRead`, and legacy
    lookup points onto `ssc.Value`. Navigable values expose total accessors,
    preserve exact decimals encoded as JSON strings, and expose compact raw JSON
    to the `.ssc` structured builders. The assembled VM/ASM smoke covers nested
    objects/arrays, missing values, booleans, stringify, escaping, and builders;
    the provider JAR passes the same static/runtime dependency gates.
  - [x] **Native YAML provider (2026-07-11, `2da4183f5`):** a dedicated
    core-free provider maps the project-owned `SimpleYaml` subset onto portable
    `Y*` values and owns deterministic sorted serialization plus total
    accessors. The self-hosted source scanner retains heading-scoped `yaml` and
    `yml` fences, including imported-module declarations, so
    `<SectionId>.yaml` resolves without `SectionRuntime` or `PluginBridge`.
    Provider tests are 3/3 and the public example is exact on VM/direct ASM/
    `build-jvm`; dependency, class-load, JRE, slim, and taxonomy gates pass.
  - [x] **Native HTTP:** add the server-host SPI and representative bounded
    server smoke. No server call may fall back to the compatibility bridge.
    - [x] **Client/response slice (2026-07-10, `69649de16`):** JDK-backed
      outbound methods, streaming callbacks, base URL, timeout/retry state,
      `Response` builders, JSON reuse, and cache helpers run on the core-free
      provider. Unit loopback GET/POST is 3/3; assembled VM/ASM builders match;
      a native `serve` smoke fails with the bounded server-host diagnostic.
    - [x] **Basic server-host slice (2026-07-10, `a81d9d94f`):** added host-owned
      callback invocation with arity/error checks, a JDK `HttpServer` lifecycle
      seam, exact method/path routes, v2 `Request` construction, handler closure
      invocation, `Response` writing, `serve`/`serveAsync`, and `stop`. Unit HTTP
      coverage is 4/4; one `.ssc` starts, calls, and stops its own server on both
      VM and ASM. `useGzip` remains a negative no-fallback gate for the advanced
      server sub-slices.
  - [x] **Native SQL provider (2026-07-10, `2528ce3e9` + `44fec39e1`):** explicit
    root `databases:` configs pass through the core-free SPI into a lazy
    `backendSqlRuntime` registry. Named H2 DDL/DML, parameter binds, and map-row
    reads pass provider tests plus assembled VM/direct-ASM execution; malformed
    and conflicting config, ServiceLoader, static dependency, runtime class-load,
    and no-Scala-CLI gates are covered. Typed writes, LISTEN/NOTIFY, and fenced
    SQL lowering remain explicit follow-ups.
  - [x] **Native UI provider (2026-07-10, `145505252`, fetch descriptors in
    `d6b9ae9ce`):** core-free mutable and
    derived signals, callback updates, event descriptors, and representative
    text/signal/show/fragment/element values feed a deterministic escaped UTF-8
    static `emit`. Provider tests are 2/2; assembled VM/direct-ASM emit identical
    `index.html` bytes with Scala CLI absent; ServiceLoader, `jdeps`, and runtime
    class-load gates reject `frontendCore`, v1, bridge, and Scalameta edges.
    Declarative fetch signals/actions now also survive standard JVM/static
    evaluation without performing network I/O or falling back to v1. Framework
    serving/codegen and advanced widgets remain explicit follow-ups.
  - [x] **Native State effect provider (2026-07-10, `0fad7cbbb` +
    `250a52da1`):** `NativePluginContext.withEffect` owns nested push/pop and
    exception cleanup; the provider owns `State.get/set/modify`, invokes modify
    callbacks through the host, and returns `(finalState, bodyResult)`. SPI tests
    are 6/6 and provider tests 2/2; assembled VM/direct-ASM nested-state output
    is identical with Scala CLI absent; dependency/class-load gates reject v1
    block forms, interpreter values, the bridge, and Scalameta.
  The assembled standard lane now covers every TI-5 representative family on
  both VM and direct ASM. Advanced provider surfaces listed above remain named
  follow-ups, but no longer block the representative boundary acceptance gate.
- [x] **TI-6 ASM artifacts (2026-07-10, through `a8e6742fa`):** `ssc build-jvm`
  produces reproducible, linked, source-mapped JARs that run without any
  compiler. The release gate is wired into CI and records the artifact hash,
  size, module graphs, hidden-tool proof, and forbidden-reference count.
- [x] **TI-7 Slim distribution (2026-07-10, through `65773c2fe`):** standard and
  tools tiers are physically separated; VM, direct ASM, representative native
  providers, and `build-jvm` survive physical deletion of the tools tier. The
  stable release gate finds zero compiler/Scalameta/v1 forbidden references.
- [x] **TI-8 No-javac cutover:** standard execution has no v1 JIT edge and passes
  without `java.compiler`/`jdk.compiler`.
  - [x] **JRE-shaped module gate (2026-07-10, `e4cd55b36`):** every standard JAR
    is a `jdeps` root, the standard H2 driver omits its eight optional compiler
    classes, and VM/direct ASM/providers/generated SQL JAR pass with a derived
    `--limit-modules` set where both compiler modules are unresolvable.
  - [x] **Default-cutover readiness:** native-front and standard
    VM/direct-ASM reports classify all 195 files. Separate the remaining 68
    sentinels and 60 both-fail rows into
    standard deterministic gaps versus explicit tools/backend/server surfaces.
    - [x] **Backend one-sided rows (2026-07-10, through `d6b9ae9ce`):** braced
      interpolation, local direct-ASM recursion, multiline function types,
      closure arity, and declarative UI fetch values close `index.ssc`,
      `recursion.ssc`, and `ui-fetch-json.ssc`. The full standard sweep is now
      11 identical / 0 mismatch / 0 one-sided / 96 both-fail / 88 skipped.
    - [x] **Frontend host errors (2026-07-10, `d4513cb8a`, `ac441ef62`):**
      multiline lambda layout, nested tuple shape, and the stale RDF table
      import are corrected. Both former crash rows now complete frontend and
      checker processing; remaining sentinels produce input-named diagnostics.
      Full corpus: 194 frontend OK / 0 error / 0 timeout / 1 non-code.
    - [x] **Stable sentinel taxonomy (2026-07-10, `aa9b30f28`, refined in
      `c82913f7b`, `4bcf6a976`, `7a1802261`, `ea805bf22`, `6440860f7`,
      `91a955171`, `e87a3aab2`, `3ddbe8d1d`, `c018ad6a1`, `07c1d9b55`,
      `23fca32a0`, `7aee8394e`, `6bdfb2ff4`, `1f50dcaa8`, and
      `063c64dcd`):** the native-front/parity join classifies all 68 sentinel
      rows as 0 standard gaps, 26 server, 36 backend, 5 explicit tools/backend, and 1
      nondeterministic row. Reviewed overrides and per-category ceilings reject
      stale exceptions, unknown rows, or category growth. Server detection
      covers `serve {}` and named `serveX` entrypoints before executing either
      backend; documents containing only platform fenced blocks are classified
      as backend surfaces without a manual override. Sentinel readiness remains
      authoritative even when both lanes exit zero identically. Current
      standard-tier parity is 10 identical / 60 both-fail / 125 skipped with no
      mismatch or one-sided
      error.
  - [x] **Default launcher cutover (2026-07-12, `e28560761`, harness
    `7ed7c630e`/`849907875`):** plain `ssc` selects StandardMain and the physical
    standard graph; compatibility/compiler lanes require explicit `ssc-tools`
    and neither standard launcher delegates. Staged, contributor, and
    self-install paths plus slim/JRE/build-jvm/exhaustive gates are green.
- [x] **TI-9 Release gates (2026-07-12, `43fded0f9`):** CI and the consolidated
  release run a copied standard-only exhaustive environment. Compiler/Scalameta
  JARs and compiler commands/modules are absent; frontend/checker, VM/ASM,
  providers/server, zero-blocker freeze, and report-validator self-tests pass.

## 9. Decisions and rejected alternatives

- **Keep Scala as implementation/runtime language in 2.1.** Removing
  `scala-library` is independent of removing runtime compiler dependencies and
  would obscure the critical path.
- **Require the native checker.** Parser-only replacement was rejected because it
  would silently weaken the typed-language contract.
- **Use the v2 CoreIR ASM emitter.** Promoting the old v1 `AsmJitBackend` was
  rejected because its input/runtime graph remains coupled to v1 frontend values.
- **Add `build-jvm`.** Reusing `compile-jvm --bytecode` unchanged was rejected
  because that command currently means generated Scala plus compiler tooling.
- **Split the distribution.** Deleting every compiler-backed feature in one
  release was rejected; explicit tools-tier Scala fences, Spark, Scala.js/WASM,
  facades, and rollback remain useful and honest.
- **Use `ssc.Value` as the native provider value boundary.** Reusing the v1
  `PluginValue` facade was rejected because its constructors/extractors are
  backed by `scalascript.interpreter.Value` and therefore pull `core` plus
  Scalameta into every provider. Inventing another closed value ADT was rejected
  because the v2 runtime already models closures, effects, records,
  collections, and opaque handles required by native providers.
- **No transparent fallback.** Falling back to Scalameta or javac after a native
  failure was rejected because it makes production dependency guarantees
  untestable and deployment-specific.

## 10. Non-goals

- compiling the ScalaScript repository from source without scalac;
- removing the Scala runtime from produced applications;
- rewriting Spark, Scala.js/WASM, or arbitrary Scala/Java fences to ASM in this
  milestone;
- removing the compatibility tier before the native path has a release of field
  evidence;
- changing target-independent `.ssc` semantics to accommodate an ASM limitation.

## 11. Verification record

Before implementation, the affected baseline gate is:

```text
scripts/sbtc "installBin"
tests/conformance/run.sh --only 'v2-*'
Results: 8 passed, 0 failed out of 8 tests
```

Per-slice commands and measured counts are appended here when each behavior item
is checked. The final release record must include exact classpath sizes, `jdeps`
output, corpus category counts, and reproducibility hashes.

### TI-6 direct ASM artifacts — executable and linked slices

`a8a86fffe` introduced the deterministic executable JAR and `147531fa7`
extended it to the complete native module closure plus serialized runtime
configuration. The SQL dependency set is conditional on parsed database
configuration; normal H2 DDL/DML/query classes remain present, while its
optional `org.h2.util.SourceCompiler*` family is deliberately excluded so the
artifact has no `javax.tools`/`java.compiler` edge. `e4f16baaf` then added
source-coordinate propagation and corrected the source identity set from only
explicit roots to the actual linked import closure.

Verification after rebasing on current `origin/main`:

```text
tests/e2e/v21-build-jvm-smoke.sh: PASS
  two base builds byte-identical
  imported relative module -> 42
  configured H2 SQL -> 1 / 7 / Ada / true
  artifact metadata records 2 base sources and the named database/provider
  jdeps finds no javax.tools, java.compiler, or jdk.compiler edge in SQL JAR
scripts/sbtc "v2NativePluginSpi/test": 7/7
tests/conformance/run.sh --only 'v2-*' --no-memo: 8/8
```

Source-map/import-closure verification:

```text
javap -l -v ssc.gen.Entry:
  SourceFile: "argv.ssc"
  LineNumberTable present
  SourceDebugExtension/SMAP names argv.ssc, std-crypto.ssc, std/crypto.ssc
relative import metadata: source.count=2; helper + root names and exact SHA-256
deliberate runtime failure: ssc.gen.Entry.explode(source-map-failure.ssc:4)
absolute checkout path gate: clear
tests/e2e/v21-build-jvm-smoke.sh: PASS (two JARs byte-identical)
tests/e2e/v21-native-entry-smoke.sh: PASS (VM + in-memory direct ASM)
tests/e2e/v21-native-plugin-boundary-smoke.sh: PASS
scripts/sbtc "v2NativePluginSpi/test": 7/7
scripts/sbtc "cli/Test/testOnly scalascript.cli.CommandRegistryTest": 8/8
tests/conformance/run.sh --only 'v2-*' --no-memo: 8/8
```

The native in-memory bytecode lane now also initializes a fresh generated
global map and relies on `JvmByteGen.install()`, matching persisted artifacts.
It no longer invokes the VM compiler as a side-effecting prepass.

### TI-6.4 artifact release gate

Command:

```text
tests/e2e/v21-build-jvm-release-gate.sh --report target/v21-build-jvm-release.tsv
```

Current reproducible baseline (self-hosted JSON cutover plus direct-ASM local
recursion and declarative UI fetch parity through `d6b9ae9ce`):

```text
artifact.bytes: 24777543
artifact.sha256: ee47e75d07ea980d1075c55397e2a07b543dafe55014d03a49b30d5549c32392
artifact.reproducible: true (different clean source/output directories)
app.modules: java.base,java.net.http,jdk.httpserver,jdk.unsupported
sql.modules: java.base,java.desktop,java.instrument,java.management,java.naming,
             java.net.http,java.scripting,java.security.jgss,java.security.sasl,
             java.sql,jdk.httpserver,jdk.unsupported
compiler commands visible in sanitized PATH: 0
forbidden entries/references/modules: 0
hello/import/argv+crypto/SQL: PASS
tests/conformance/run.sh --only 'v2-*' --no-memo: 8/8
```

Neither module graph contains `java.compiler` or `jdk.compiler`. The forbidden
scan also rejects Scalameta, `scala.tools`, `dotty.tools`, compiler drivers,
`javax.tools`, the compatibility bridge, and v1 AST/frontend/interpreter
families. CI runs this gate immediately after `installBin` and before the broad
sbt suite.

### TI-7.3 slim-distribution deletion gate

Command:

```text
tests/e2e/v21-slim-distribution-gate.sh \
  --report target/v21-slim-distribution.tsv
```

The gate copies the complete staged `bin/lib` tree, then deletes `jars/`,
`compiler/`, the legacy `native-front/`, the full `ssc.jar`, and `ssc-tools`.
Its sanitized `PATH` contains only `bash`, `dirname`, and `java`;
`scala-cli`, `scalac`, and `javac` are therefore unavailable even if installed
on the build host. The copied default `ssc`, its `ssc-standard` alias, and
`lib/standard/**` remain reachable.

Current stable baseline after TI-8.3:

```text
standard.jar.count: 40
standard.class.count: 6684
standard.bytes: 31178916
default.launcher: standard
tools.present: false
compiler.commands.hidden: true
forbidden.references: 0
standard.vm: PASS
standard.asm: PASS
standard.providers: fs-os,json,http,sql,ui,state,effect-runners,storage,reactive,yaml,content,dataset,generator,actors,distributed,graph,optics
standard.build-jvm: PASS
tests/e2e/v21-standard-tier-smoke.sh: PASS
tests/e2e/v21-build-jvm-release-gate.sh: PASS
tests/e2e/v21-default-launcher-cutover-smoke.sh: PASS
tests/conformance/run.sh --only 'v2-*' --no-memo: 11/11
```

The recursive JAR/`jdeps` scan rejects Scalameta, `scala.tools`, `dotty.tools`,
`javax.tools`, `java.compiler`, `jdk.compiler`, `ssc.bridge`, and the v1
AST/frontend/interpreter families. A class-load trace applies the same policy to
runtime reachability. A plain `ssc run --v1` request fails before execution with
the bounded tools-tier remedy even when the full tier is present; explicit
compatibility uses `ssc-tools`. TI-8.1 strengthened
this scan after finding that starting only from `standard/ssc.jar` did not make
ServiceLoader providers and JDBC drivers roots; all 33 JARs are now merged into
a scan-only archive before `jdeps` runs.

### TI-8.1 JRE-shaped no-javac module gate

Command:

```text
tests/e2e/v21-jre-module-gate.sh --report target/v21-jre-module.tsv
```

The gate derives its module set from a scan-only union of every standard-tier
JAR, rejects either compiler module in that set, and uses the resulting list for
all Java invocations through `java --limit-modules`. It separately proves that
`--describe-module java.compiler` and `jdk.compiler` fail as not found. The
sanitized `PATH` also hides Scala CLI, scalac, and javac.

Current baseline (`e4cd55b36` inputs):

```text
runtime.modules: java.base,java.desktop,java.instrument,java.management,
                 java.naming,java.net.http,java.scripting,java.security.jgss,
                 java.security.sasl,java.sql,jdk.httpserver,jdk.net,
                 jdk.unsupported
java.compiler.available: false
jdk.compiler.available: false
compiler.commands.hidden: true
forbidden.references: 0
standard.vm: PASS
standard.asm: PASS
standard.providers: fs-os,json,http,sql,ui,state
standard.build-jvm: PASS (module-limited H2 SQL artifact)
```

The audit also closed a false-negative in TI-7: H2 2.2.224 contains eight
optional `org/h2/util/SourceCompiler*` classes whose constant pools reference
`javax.tools`. `installBin` now deterministically omits only those classes from
`lib/standard/jars/h2-2.2.224.jar` while retaining the complete driver in
`lib/jars` for tools compatibility. Normal H2 DDL/DML/query passes on the native
VM, direct ASM, and generated artifact under the module limit. The strengthened
slim gate, self-hosted core dependency gate, standard smoke, artifact hash gate,
and affected conformance remain green.

### TI-8.2 direct-ASM local recursion

Command:

```text
tests/e2e/v21-direct-asm-recursion-smoke.sh
```

Before `3153fb2db`, top-level self/mutual calls already used bytecode loops or
the `Bounce` trampoline, but a local `LetRec` call fell through `Emit.app` and
recursively entered `Runtime.run`. `recursion.ssc` therefore overflowed while
building `(1 to 10000).toList` in the local `_sel_to` helper, after printing
only its first four rows.

The emitter now records every local recursion group's generated target methods,
their arities, and environment-relative indexes. A tail call retains captured
values plus the tied closure group, replaces the current argument suffix with
the target arguments, and returns the same compiler-free `Bounce` used by
top-level mutual recursion. Non-tail calls retain ordinary call semantics.

Verification:

```text
focused bytecode tests: 3/3
  arithmetic loop, non-tail recursive fib, 100000-call local self/mutual TCO
recursion.ssc VM/direct ASM/build-jvm: byte-identical 13 rows at -Xss256k
scripts/bc-parity-sweep --ssc bin/ssc-standard --only recursion.ssc --strict:
  identical=1; mismatch/bytecode-error/vm-error/both-fail=0
native-entry/standard/slim/JRE-module/core-dependency/artifact gates: PASS
tests/conformance/run.sh --only 'v2-*' --no-memo: 8/8
```

### TI-8.2 multiline helper and UI fetch parity

The native type skipper originally treated the inner `)` of a function type
such as `body: () => String` as the end of the surrounding parameter list. The
imported `fetchJsonValue`/`fetchJsonAction` declarations therefore lowered with
truncated arities and empty bodies. Direct ASM compounded the problem by
accepting malformed closure applications that the VM rejected.

`d6b9ae9ce` balances both square brackets and parentheses in skipped types,
checks closure arity identically in `Emit.app`, and installs declarative native
UI fetch values. Browser network execution is deliberately not simulated by
the JVM/static lane.

```text
multiline function-typed parameter VM/direct ASM: [typed]
ui-fetch-json.ssc VM/direct ASM:
  body:{"name":"Acme \\"HQ\\"","n":5}
  fetch-json:ok
focused strict parity: identical=1; all error categories=0
focused bytecode tests: 4/4 (including arity rejection)
native UI provider tests: 3/3
native-entry/standard/slim/JRE-module/core-dependency/artifact/JSON gates: PASS
tests/conformance/run.sh --only 'v2-*' --no-memo: 8/8
```

### TI-8.2 current readiness corpus

Commands:

```text
scripts/native-front-corpus --report target/v21-native-front-current.tsv
scripts/bc-parity-sweep --ssc bin/ssc-standard \
  --report target/v21-standard-bc-parity-current.tsv
```

Current 195-file baseline after final mini-language shapes `063c64dcd`:

```text
native front: 194 OK / 0 host errors / 0 timeout / 1 non-code
native sentinel: 68 present / 127 clear
native check: 194 OK / 0 TYPEERR / 0 crash-or-timeout / 1 non-code
native run: 28 OK / 98 errors / 0 timeout; strict-fail rows=166
standard VM/ASM: 10 identical / 0 mismatch / 0 one-sided /
                 60 both-fail / 125 skipped
```

The one-sided backend and frontend host-error queues are empty.
`dsl-mini-language.ssc` and `graph-fullstack-rdf.ssc` now reach controlled,
input-named sentinel diagnostics and are part of the TI-8.2c syntax taxonomy.
Both-fail is deliberately not counted as successful parity.

### TI-8.2c sentinel taxonomy gate

Commands:

```text
scripts/v21-sentinel-taxonomy \
  --native-report target/v21-native-front-current.tsv \
  --parity-report target/v21-standard-bc-parity-current.tsv \
  --report target/v21-sentinel-taxonomy-current.tsv
tests/e2e/v21-sentinel-taxonomy-smoke.sh
```

Current complete classification:

```text
standard-gap:     0
server:          26
backend:         36
tools-backend:    5
nondeterministic: 1
total:           68
```

The gate inherits source-derived parity categories, while
`tests/fixtures/v21-sentinel-taxonomy/overrides.tsv` names only reviewed
compiler/target exceptions that the source classifier cannot infer. Every
override must remain sentinel-bearing; disappearance or readiness
reclassification fails until the manifest is deliberately updated. Parity is
an independent axis: an uncalled or non-observable `_err` may let VM and direct
ASM exit zero identically, but it remains a parser gap. After source-derived
server/backend/nondeterministic categories and reviewed overrides are applied,
every other sentinel is `standard-gap` whether parity says `both-fail` or
`identical`. Category ceilings may shrink without churn, but growth fails. The
standard-gap rows are the only TI-8.2c2 parser queue; server/backend/tools rows
do not justify a fallback to Scalameta or compiler tooling in the standard
launcher.

`x402-client.ssc` is an explicit tools/backend row: its regular `scalascript`
fence imports `scala.concurrent`, sttp, and compiler-backed URI interpolation.
Those platform APIs are forbidden in standard `.ssc` code; the reviewed
override remains bound to a sentinel-bearing row so a future portable rewrite
fails stale and returns to ordinary standard classification.

Taxonomy-axis acceptance:

- [x] synthetic `identical` parity rows remain classifiable as both an ordinary
  standard sentinel and a reviewed tools/backend sentinel;
- [x] the real current frontend/parity reports classify every sentinel with no
  stale override, regardless of whether its parity row is `both-fail` or
  `identical`.

#### TI-8.2c2a decimal separators and long suffixes

The self-hosted lexer accepts `_` inside a decimal digit sequence when the
separator is followed by another digit. Separators are removed from the token
payload before lowering. An optional final `L` or `l` remains an erased source
suffix because the v2 CoreIR integer representation is already target-neutral.
An underscore at the start/end of a literal or two consecutive underscores is
not consumed as part of the numeric token.

Acceptance:

- [x] `100_00L` lowers as the integer `10000` without `_err`.
- [x] a real native launcher fixture prints `10000` identically on VM and direct
  ASM;
- [x] `international-bank-rails.ssc` no longer has a numeric-literal parser
  sentinel, and the 195-file taxonomy is rerun before the slice closes.

#### TI-8.2c2c triple-quoted strings

Three consecutive quote characters open a raw multiline string and the next
three consecutive quote characters close it. The token payload preserves
embedded newlines, ordinary quote characters, and backslashes without the
escape processing used by an ordinary quoted string. The existing `s` prefix
still applies interpolation after the lexer has produced the complete string
token.

Acceptance:

- [x] a real native launcher fixture preserves a multiline body with embedded
  quotes identically on VM and direct ASM;
- [x] all three SPARQL literals in `graph-rdf4j-http-storage.ssc` lex as single
  string tokens and the document becomes sentinel-clear/checker-OK;
- [x] the 195-file native-front, parity, and sentinel-taxonomy reports are rerun
  before the slice closes.

#### TI-8.2c2d enum declaration boundary

An enum body consumes consecutive `case Name` / `case Name(...)` entries, but a
following `case class` begins a new top-level declaration. The self-hosted parser
must stop the enum-case scan before the `case class` pair; `class` is a keyword,
not a legal enum case identifier.

Acceptance:

- [x] a real native launcher fixture containing an enum followed by generic
  `case class Box[A]` constructs both values identically on VM and direct ASM;
- [x] `typed-data.ssc` emits a `Box` constructor and contains no parser sentinel;
- [x] the full native-front and sentinel taxonomy reports are rerun before the
  slice closes.

#### TI-8.2c2e delimiter-aware lambda layout

The offside pass tracks explicit parentheses and brackets in addition to braces.
When `)` or `]` closes an explicit delimiter, every virtual layout block opened
inside that delimiter is closed first; layout blocks outside the delimiter stay
open. This makes a multiline lambda used as a call argument end before the
call's closing parenthesis without closing layout around nested calls in the
lambda body.

Acceptance:

- [x] a real multiline tuple-lambda call fixture prints the same result on
  native VM and direct ASM without `_err`;
- [x] `std/money.allocate` and `content-linked-namespaces.ssc` are
  sentinel-clear/checker-OK;
- [x] the full native-front, parity, and taxonomy reports are rerun before the
  slice closes.

#### TI-8.2c2f ordered binder match guards

A binder or wildcard match arm may include `if condition` before `=>`. The
scrutinee is evaluated once. A binder is in scope in both the condition and arm
body; when the condition is false, matching continues with the next arm in
source order. Literal and constructor arms between guarded binder arms retain
their ordinary ordered fall-through. Guarded constructor patterns are a
separate follow-up because their field scopes require nested-pattern fallback.

Acceptance:

- [x] a real guarded-classification fixture produces negative/zero/small/large
  identically on native VM and direct ASM;
- [x] `data-types.ssc` is sentinel-clear/checker-OK;
- [x] the full native-front, parity, and taxonomy reports are rerun before the
  slice closes.

#### TI-8.2c2g flat constructor match guards

A constructor arm whose immediate fields are binders or wildcards may include a
guard. Constructor fields are in scope in the guard and body. When the guard is
false, later arms match the same once-evaluated scrutinee; field locals from the
failed arm are not visible by name. Nested constructor guards remain a separate
follow-up.

Acceptance:

- [x] a real guarded `Some` fixture falls through from a false guard to a later
  `Some` arm identically on native VM and direct ASM;
- [x] `direct-syntax-demo.ssc` is sentinel-clear/checker-OK;
- [x] the full native-front, parity, and taxonomy reports are rerun before the
  slice closes.

#### TI-8.2c2h extension declaration boundary

The self-hosted parser recognizes `extension [T](receiver: Type)` as a
declaration header and resumes statement parsing at the following `def` methods.
The header itself emits no executable expression and no parser sentinel. This
bounded slice does not claim receiver binding, method dispatch, or symbolic
extension-operator semantics; those remain required before the affected
documents can move from runtime gaps to successful execution.

Acceptance:

- [x] a real uncalled-extension fixture runs identically on native VM and direct
  ASM without `_err`;
- [x] `script.ssc` is sentinel-clear/checker-OK; the extension header in
  `dsl-ast-builder.ssc` is clear while its independent `:+` gap remains;
- [x] the full native-front, parity, and taxonomy reports are rerun before the
  slice closes.

#### TI-8.2c2i portable list append

The native ScalaScript lexer recognizes `:+` as one infix token. It has the
same collection-concatenation precedence as `++`, is left-associative, and has
the type `List[A] :+ A -> List[A]`. Lowering emits the existing portable
`__arith__(":+", left, right)` primitive so VM and direct ASM share evaluation
order and immutable-list semantics. This is a core operator; it does not depend
on extension declaration binding, Scala collections, or a JVM-only intrinsic.

Acceptance:

- [x] a real native launcher fixture preserves append order and element values
  identically on VM and direct ASM;
- [x] the native checker accepts a known `List[Int] :+ Int` expression and the
  lowering contains no `_err` or fallback integer addition;
- [x] `dsl-ast-builder.ssc` is sentinel-clear/checker-OK and reaches its honest
  missing imported `Node` runtime boundary;
- [x] the full native-front, parity, and taxonomy reports are rerun before the
  slice closes, with no new mismatch or one-sided row.

Results: `tests/fixtures/v21-native/list-append.ssc` prints `1,2,3,4` on both
assembled lanes; native-entry and the fresh 9/9 `v2-*` conformance slice pass.
The corpus has 74 sentinels and 6 standard syntax gaps, and the corrected
taxonomy classifies all of them even when parity is `identical`.

#### TI-8.2c2j symbolic extension-operator syntax

The native lexer consumes `~`, `~>`, and `<~` as complete symbolic operator
tokens. Their precedence follows the operator's first character: `~`/`~>` use
the highest symbolic-infix tier and `<~` uses the comparison tier. The same
tokens are legal declaration names after `def`. A non-core symbolic infix
expression lowers to an explicit two-argument global call; it must not use the
historical unknown-operator fallback to integer addition.

This bounded slice does not claim extension receiver capture or dynamic
extension dispatch. A symbolic extension global that is not yet callable may
fail explicitly at runtime, but its declaration and call sites contain no
parser sentinel and cannot silently execute as arithmetic.

Acceptance:

- [x] a real uncalled symbolic-definition fixture parses/checks and executes a
  following ordinary expression identically on native VM and direct ASM;
- [x] `dsl-calc-parser.ssc`, `dsl-json-parser.ssc`, and
  `dsl-sql-recovery.ssc` are sentinel-clear/checker-OK;
- [x] the symbolic-operator nodes in `dsl-yaml-like.ssc` are clear; its
  independent constructor-pattern alternative remains scoped to TI-8.2c2k;
- [x] CoreIR for non-core symbolic infix forms contains an explicit global call
  and no fallback `i.add`;
- [x] the full native-front, standard parity, and taxonomy reports are rerun
  before the slice closes, with no new mismatch or one-sided row.

Results: the focused fixture prints `symbolic-operators-ok` on both assembled
lanes. Calc and JSON reach explicit missing `Parser_regex` runtime boundaries;
SQL recovery runs successfully; YAML retains only the separately specified
pattern-alternative sentinel. The corpus has 71 sentinels, checker 194/0,
runtime 28 OK / 95 errors, taxonomy 3/26/36/5/1, and standard parity
10 identical / 60 both-fail / 125 skipped with zero mismatch/one-sided.
Native-entry and fresh conformance 9/9 pass.

#### TI-8.2c2k constructor pattern alternatives

A match arm may join flat constructor patterns with `|`, for example
`case YMap(_) | YSeq(_) => body`. Each alternative participates at the same
source position and shares one arm body. The scrutinee is evaluated once; the
body is evaluated only for the selected alternative. This slice requires the
same field arity/binding shape across alternatives and does not introduce
nested alternative patterns.

Acceptance:

- [x] a focused `A(_) | B(_)` fixture selects both alternatives and a fallback
  identically on native VM and direct ASM;
- [x] `dsl-yaml-like.ssc` is sentinel-clear/checker-OK and reaches its honest
  missing `Parser_regex` runtime boundary;
- [x] the full native-front, standard parity, and taxonomy reports are rerun
  before the slice closes.

Results: the fixture prints `hit`, `hit`, `miss` on both assembled lanes. The
corpus has 70 sentinels, checker 194/0, runtime 28 OK / 96 errors, taxonomy
2/26/36/5/1, and standard parity 10/60/125 with zero mismatch/one-sided.
Native-entry and fresh conformance 9/9 pass.

#### TI-8.2c2l assignment expressions

After parsing an ordinary expression, a following single `=` forms an
assignment only when the left side is a bare variable. The right side uses the
ordinary expression grammar and evaluation returns Unit after updating the
existing local or top-level cell. This makes assignment legal inside branch and
loop bodies without changing named call arguments or equality `==`.

Acceptance:

- [x] a focused fixture mutates a variable inside both `if ... then` and
  `for ... do`, with identical native VM/direct-ASM output;
- [x] named arguments and equality retain their existing parsing regressions;
- [x] `extensions.ssc` is sentinel-clear/checker-OK; extension dispatch may
  remain an explicit runtime gap;
- [x] the full native-front, standard parity, and taxonomy reports lose the
  extension row without a mismatch/one-sided backend row.

Results: the focused fixture prints `6`, `true`, `7` identically on native VM
and direct ASM. `extensions.ssc` is sentinel-clear/checker-OK and reaches its
honest missing `.shout` dispatch boundary. The corpus has 69 sentinels, checker
194/0, runtime 28 OK / 97 errors, taxonomy 1/26/36/5/1, and standard parity
10/60/125 with zero mismatch/one-sided. Native-entry and fresh conformance 9/9
pass.

`dsl-mini-language.ssc` also exercises assignment, but its remaining sentinels
are not assignment nodes: they are the parenthesized-condition continuation and
tuple-cons pattern specified next.

#### TI-8.2c2m final mini-language shapes

An `if` condition may begin with a parenthesized term and continue with an infix
operator after `)`, as in `(a || b) && c`. Parentheses group a term; they do not
force the entire condition to end. A match pattern may also cons a tuple head,
as in `(name, pass) :: rest`; the head uses the existing right-nested tuple
pattern shape and the tail is an ordinary sub-pattern.

Acceptance:

- [x] focused fixtures prove the condition continuation and tuple-cons pattern
  identically on native VM and direct ASM;
- [x] `dsl-mini-language.ssc` is sentinel-clear/checker-OK;
- [x] the full native-front, standard parity, and taxonomy reports contain zero
  standard parser-gap rows and no mismatch/one-sided backend row.

Results: the focused fixture prints `condition-ok`, `stage`, `7`, `true`
identically on native VM and direct ASM. The parenthesized-condition path keeps
legacy `if (cond) { branch }` from treating the branch as a block argument, and
`oidc-login-flow.ssc` remains sentinel-clear/checker-OK. `dsl-mini-language.ssc`
is sentinel-clear/checker-OK and reaches its honest `expected Int, got "2"`
runtime boundary. The corpus has 68 classified sentinels, checker 194/0,
runtime 28 OK / 98 errors, taxonomy 0/26/36/5/1, and standard parity 10/60/125
with zero mismatch/one-sided. Native-entry and fresh conformance 9/9 pass.

### TI-8.2d runtime/provider readiness taxonomy

Backend agreement is necessary but not sufficient: a `both-fail` row is not a
successful standard program. Every current standard parity row whose VM and
direct ASM lanes fail together must appear exactly once in a reviewed runtime
manifest with one of these categories:

| Category | Blocks default cutover | Meaning |
|---|---:|---|
| `language-runtime` | yes | portable language/runtime semantics are missing or wrong, including lowering, dispatch, stack safety, arity/default arguments, or value conversion |
| `standard-provider` | yes | a standard-owned global/intrinsic/provider has not yet migrated to the core-free native SPI |
| `optional-provider` | no, when reviewed | an explicitly opt-in platform/plugin surface is absent and fails with a stable provider diagnostic rather than compatibility fallback |
| `example-contract` | yes | the deterministic example has stale imports, setup, data, or configuration that prevents its declared standard behavior |
| `tools-backend` | no | the source is already a reviewed compiler/platform-specific tools or backend surface, consistent with the sentinel taxonomy |

The durable inputs are
`tests/fixtures/v21-runtime-taxonomy/manifest.tsv` and
`category-limits.tsv`. The manifest records `file`, `category`, `blocker`,
`owner`, and a concrete reason. `scripts/v21-runtime-taxonomy` joins it against
the current standard parity and sentinel-taxonomy reports and writes
`target/v21-runtime-taxonomy-current.tsv`.

Normative rules:

1. Every `both-fail` row is present exactly once; an unknown, duplicate, stale,
   no-longer-`both-fail`, or category-growing row fails the gate.
2. `language-runtime`, `standard-provider`, and `example-contract` entries are
   always blockers. Their ceilings may only shrink.
3. `optional-provider` is non-blocking only when the source makes the optional
   platform/plugin ownership explicit and the diagnostic is bounded. Otherwise
   the row remains `standard-provider`.
4. `tools-backend` must agree with the already-reviewed sentinel/backend
   classification; it cannot hide sentinel-clear standard runtime debt.
5. Default launcher cutover requires zero blocking rows, zero VM/ASM mismatch or
   one-sided errors, and exact frozen counts for the remaining reviewed
   optional/tools rows.

Acceptance:

- [x] a synthetic smoke rejects unknown, duplicate, stale, invalid-blocker, and
  category-growing rows;
- [x] all 60 current `both-fail` rows have a concrete category, owner, reason,
  and blocker decision in the generated report;
- [x] the initial exact category/blocker counts and verification commands are
  recorded before implementation work starts shrinking the blocker set.

Initial result (`df84e8acd`):

```text
scripts/v21-runtime-taxonomy:
  example-contract:    3
  language-runtime:   20
  optional-provider:   6
  standard-provider:  25
  tools-backend:        6
  blocker-total:       48
  total both-fail:     60
tests/e2e/v21-runtime-taxonomy-smoke.sh: PASS
tests/conformance/run.sh --only 'v2-*' --no-memo: 10/10
```

The generated report retains the original parity diagnostic beside the reviewed
owner/reason, so later slices shrink a measured blocker ceiling rather than
silently relabeling failures. The 12 non-blocking rows are six explicitly
optional providers and six compiler/platform tools surfaces; neither category
permits compatibility fallback. The ownership correction `6b736d078` moved
three content externs from the module-linker bucket to the standard content
provider without changing the 48-blocker total.

#### TI-8.2d2a multiple Markdown imports per line

A standalone Markdown import line may contain one or more
`[names](path.ssc)` links separated only by whitespace. The native module loader
must resolve every link in source order through the existing normalized DFS and
load-once rules. If any non-whitespace prefix, suffix, or text occurs on the
line, the entire line remains prose and contributes no imports; accepting a
leading import while silently discarding a prose tail is forbidden.

Acceptance:

- [x] a multi-file assembled fixture imports definitions from two modules on one
  line and produces identical native VM/direct-ASM output;
- [x] existing single-link imports and prose/inline-code link rejection retain
  their native-entry regressions;
- [x] `multi-link-imports.ssc` resolves both imported modules and advances from
  the false `minorUnits` linker error to its independent exact-Decimal boundary;
  the runtime manifest transfers the row to the Decimal owner without changing
  the blocker total;
- [x] full parity, runtime taxonomy, native-entry, and fresh conformance gates
  pass with no mismatch/one-sided row.

Exact Decimal construction, BigInt conversion, rounding constants, and
type-directed arithmetic already have a compiler-free `DecimalV`/`dec.*`
runtime contract, but the self-hosted lowerer does not yet emit it. That
cross-cutting numeric slice is specified and verified separately; broadening the
lexical import fix until money happens to run would mix independent root causes.

Results (`836ceee03`, `64fcab537`): the three-file fixture imports two helper
modules from one line and prints `42` identically on assembled VM/direct ASM.
The real example now resolves `money`, `minorUnits`, `Right`, and `foldEither`,
then fails identically at `unbound global: Decimal`. Runtime taxonomy is 19
language-runtime / 26 standard-provider / 6 optional-provider / 3
example-contract / 6 tools-backend, with the unchanged 48-blocker/60-total
ceilings. Native corpus remains 194/0 frontend/checker with 68 classified
sentinels; standard parity is 10/60/125 with no mismatch/one-sided;
native-entry and fresh conformance 10/10 pass.

#### TI-8.2d2b exact Decimal and BigInt lowering

The self-hosted standard lowerer must use the existing target-neutral exact
numeric CoreIR contract rather than treating `Decimal` and `BigInt` as runtime
globals. `Decimal(text)` lowers to `dec.parse`; `Decimal(unscaled, scale)` lowers
to `dec.from-unscaled`; and `BigInt(int)` lowers to `i->big`. Rounding-mode
members such as `RoundingMode.HALF_UP` lower as portable symbolic values already
accepted by `dec.set-scale`.

Because parsed expression variables do not retain enough local type information
to select `i.*` versus `dec.*`, ordinary `+`, `-`, `*`, `/`, `%`, and ordered
comparisons lower through the shared `__arith__` primitive. The VM and direct
ASM optimizer may still specialize proven integers, but frontend lowering must
not bake in an Int-only operation for a Decimal/BigInt expression. Structural
equality remains `__eq__`.

Acceptance:

- [x] a focused native VM/direct-ASM fixture proves exact parsing, HALF_UP scale,
  BigInt power/conversion, Decimal arithmetic/comparison, and scale-preserving
  text without binary floating-point conversion;
- [x] the imported `std/money.ssc` path runs `multi-link-imports.ssc` identically
  as `minor units: 1234` on both assembled backends;
- [x] full native-entry, corpus, parity, runtime-taxonomy, and fresh conformance
  gates pass; the money row leaves the blocker set and no Decimal/BigInt change
  creates a mismatch or one-sided failure.

Results (`e4a9282d7`): `exact-decimal.ssc` produces exact, identical VM/direct-
ASM output for parsing, HALF_UP rounding, unscaled construction, addition,
multiplication, comparison, BigInt conversion, and text concatenation. The real
three-module money example now prints `minor units: 1234` on both lanes. The
native corpus remains 194/0 frontend/checker with 29 runtime successes, 97
honest runtime errors, and 68 classified sentinels (0 standard gaps). Standard
parity improves to 11 identical / 59 both-fail / 125 skipped with zero mismatch
or one-sided failure. Runtime taxonomy tightens to 19 language-runtime / 25
standard-provider / 6 optional-provider / 3 example-contract / 6 tools-backend,
with 47 blockers and 59 total rows. Native-entry, taxonomy smoke, and fresh
conformance 10/10 pass.

#### TI-8.2d2c native default arguments

The self-hosted frontend must retain the ordered parameter names and optional
default AST for top-level functions, case-class constructors, and parameterized
enum cases. A positional call that omits only a suffix whose every parameter has
a default is expanded before CoreIR lowering; required omissions and over-arity
remain honest fixed-arity runtime errors.

Default expressions are evaluated left-to-right in a scope containing every
earlier materialized parameter. The expansion uses nested single-argument
lambdas, for example `shift(10)` becomes the semantic equivalent of
`(x => (by => shift(x, by))(x + 1))(10)`. This preserves single evaluation of
provided/default expressions and avoids introducing a target-specific optional-
argument ABI. Direct AST substitution is rejected because it duplicates effects
when a later default references an earlier argument.

Named-argument default filling and defaults on instance methods remain separate
follow-ups; this slice preserves their existing behavior and must not claim them
from the two positional blocker rows.

Acceptance:

- [x] a focused VM/direct-ASM fixture covers zero, one, and fully supplied
  function arguments, an earlier-parameter reference, case-class defaults, and
  parameterized enum-case defaults;
- [x] `default-params.ssc` and `typed-data.ssc` leave the runtime blocker set or
  advance only to independently classified later boundaries on both lanes;
- [x] required omissions and over-arity still fail honestly, while native-entry,
  corpus, parity, taxonomy, and fresh conformance remain green with no mismatch
  or one-sided failure.

Results (`afb11b082`): the focused fixture proves left-to-right, single-
evaluation defaults for functions, earlier-parameter references, case classes,
and enum cases on VM/direct ASM; dedicated negative fixtures retain honest
under-/over-arity failures. `default-params.ssc` now runs completely and
identically. `typed-data.ssc` consumes its `Person.email = None` constructor
default and advances to the independent nested `Some/1` pattern boundary.
Native corpus improves to 30 successes / 96 honest runtime errors with 194/0
frontend/checker and 68 classified sentinels. Standard parity is 12 identical /
58 both-fail / 125 skipped with zero mismatch or one-sided failure. Runtime
taxonomy tightens to 18 language-runtime / 25 standard-provider / 6 optional-
provider / 3 example-contract / 6 tools-backend, with 46 blockers and 58 total
rows. Native-entry, taxonomy smoke, and fresh conformance 11/11 pass.

#### TI-8.2d2d collection companion calls

The self-hosted resolver must represent the portable `List`, `Seq`, `Vector`,
and `Array` companion receivers as their existing zero-field method-object
values, not unresolved globals/effects. First-order companion methods such as
`range(from, until)` continue through `__method__` unchanged.

Scala-style curried factories `tabulate(n)(f)` and `fill(n)(value)` are source
sugar over the existing two-argument portable method contract. The resolver
flattens exactly those known nested calls into one method application before
CoreIR lowering. A generic application-of-application rewrite is rejected
because ordinary functions may intentionally return functions.

Acceptance:

- [x] a focused VM/direct-ASM fixture covers `List.tabulate`, nested tabulation,
  `List.fill`, `List.range`, and the equivalent `Array.tabulate`/`Array.fill`
  mutable-array results;
- [x] `lang-split.ssc` leaves the `List.tabulate` blocker and either runs or
  advances identically to an independently classified later boundary;
- [x] native-entry, corpus, parity, runtime taxonomy, and fresh conformance pass
  with zero mismatch or one-sided failure, and the old collection blocker row is
  removed or honestly reclassified.

Results (`69a0b2a51`): the focused fixture runs list/array tabulate and fill,
nested tabulation, first-order range, and mutable-array indexing identically on
VM/direct ASM. `lang-split.ssc` now prints both 3×3 tables and advances to its
independent standard `math` provider boundary on both lanes. Native corpus stays
194/0 frontend/checker, 30 successes / 96 honest runtime errors, and 68
classified sentinels. Standard parity remains 12 identical / 58 both-fail / 125
skipped with no mismatch or one-sided row. Runtime taxonomy transfers the row
from language-runtime to standard-provider: 17 / 26 / 6 / 3 / 6, with 46
blockers and 58 total rows unchanged. Native-entry, taxonomy smoke, and fresh
conformance 11/11 pass.

#### TI-8.2d2e extension receiver dispatch

For the current self-hosted source subset, an `extension (receiver: T)` header
owns the contiguous `def` declarations that immediately follow it. The frontend
prepends the receiver name to each generated function parameter list and records
the method name in a shared extension registry. The registry is reset for every
program and the extension group closes at the first non-`def` statement or the
next extension header.

Property-style `value.method` and call-style `value.method(args...)` then lower
to the registered global function with the receiver prepended. Unique method
names are required in this slice; type-directed overload selection, nested/local
extensions, and a top-level ordinary `def` placed immediately after an extension
group without an intervening statement remain out of scope. A generic
`__method__` runtime fallback is rejected because it loses the lexical receiver
binding and makes extension availability target-dependent.

Acceptance:

- [x] a focused VM/direct-ASM fixture covers property-style and argument-taking
  String/Int extensions plus a List receiver and proves the group closes before
  an ordinary following definition;
- [x] `extensions.ssc` and `script.ssc` leave their missing-extension boundaries
  and either run or advance identically to independently classified gaps;
- [x] native-entry, corpus, parity, taxonomy, and fresh conformance pass with no
  mismatch/one-sided row, and both obsolete extension blocker rows are removed
  or honestly reclassified.

Results (`0a89b861d`, parity hardening `2769bc479`): the focused String/Int/List
fixture and `script.ssc` run completely and identically on VM/direct ASM.
`extensions.ssc` executes every extension call and advances to the independent
List-length lowering boundary. Native corpus improves to 31 successes / 95
honest runtime errors with 194/0 frontend/checker and 68 classified sentinels.
The deterministic standard report is 12 identical / 57 both-fail / 126 skipped
with no mismatch or one-sided row; live `v2-http-sql-demo.ssc` is now skipped
before either backend executes. Runtime taxonomy tightens to 16 language-runtime
/ 26 standard-provider / 6 optional-provider / 3 example-contract / 6 tools-
backend, with 45 blockers and 57 total rows. Native-entry, portable/taxonomy
smokes, and fresh conformance 11/11 pass.

#### TI-8.2d2f dynamic length and size dispatch

The self-hosted resolver may emit the direct `slen` primitive only when the
receiver is statically recognizable as a String expression. Known list values
and list constructions retain their generated `_sel_length` path. Every other
`.length`/`.size` receiver lowers through the existing portable `__method__`
contract, whose VM/direct-ASM implementation already distinguishes String,
Cons/Nil, and mutable Array values and fails honestly for unsupported values.

Treating every unknown receiver as String is rejected: type erasure makes
function and extension receiver parameters unknown at this stage, and `slen`
turns a valid List length into a false String conversion failure.

Acceptance:

- [x] a focused VM/direct-ASM fixture covers literal/dynamic String, List, and
  Array `.length`/`.size`, while an unsupported receiver fails on both lanes;
- [x] `extensions.ssc` leaves the false `slen` boundary and either runs or
  advances identically to a separately classified later gap;
- [x] native-entry, corpus, parity, taxonomy, and fresh conformance pass without
  mismatch/one-sided rows, and the obsolete length blocker is removed or
  honestly reclassified.

Results (`5a4e7fd45`): literal and dynamic String, List, and mutable Array
length/size cases agree on VM/direct ASM; Boolean length fails honestly on both.
`extensions.ssc` now completes its average, comprehension, nested generator, and
for-loop output before advancing to the independent `while` lowering gap. Native
corpus stays 194/0 frontend/checker, 31 successes / 95 honest runtime errors, and
68 classified sentinels. Deterministic standard parity remains 12 identical /
57 both-fail / 126 skipped with no mismatch or one-sided row. Runtime taxonomy
stays 16 language-runtime / 26 standard-provider / 6 optional-provider / 3
example-contract / 6 tools-backend, with 45 blockers / 57 total rows; only the
row's root-cause owner changes. Native-entry, taxonomy smoke, and fresh
conformance 11/11 pass.

#### TI-8.2d2g top-level while statements

`while condition do body` and `while (condition) body` at document scope use
the same parsed `while` statement node and target-neutral `IrWhile` contract as
loops inside blocks. The top-level parser must retain the statement instead of
treating `while` as an expression/global, and the program-entry builder must
sequence it in document order with top-level `val`/`var`, assignments, and
expression statements.

Loop conditions and bodies reference the existing cell-backed top-level mutable
variables; no separate global-loop runtime primitive is introduced. Lowering a
top-level loop through the existing single-statement block path is chosen to
reuse assignment/cell semantics and rejected alternatives are duplicating the
`IrWhile` lowering or interpreting the loop in the loader.

Acceptance:

- [x] a focused VM/direct-ASM fixture covers zero-iteration and counted loops,
  multiple body assignments, and statements before/after the loop;
- [x] `extensions.ssc` leaves the false `unbound global: while` boundary and
  either runs or advances identically to a separately classified later gap;
- [x] native-entry, corpus, parity, taxonomy, and fresh conformance pass without
  mismatch/one-sided rows, and the obsolete while blocker is removed or
  honestly reclassified.

Results (`d626f00a6`): zero-iteration, counted, multi-assignment, and nested-body
top-level loops agree on VM/direct ASM and preserve document order plus global
cell mutation. `extensions.ssc` now completes on both lanes. Native corpus
improves to 32 successes / 94 honest runtime errors with 194/0 frontend/checker
and 68 classified sentinels. Deterministic strict parity improves to 13
identical / 56 both-fail / 126 skipped with no mismatch or one-sided row.
Runtime taxonomy tightens to 15 language-runtime / 26 standard-provider / 6
optional-provider / 3 example-contract / 6 tools-backend, with 44 blockers and
56 total rows. Native-entry, taxonomy smoke, and fresh conformance 11/11 pass.

#### TI-8.2d2h layout given-object bodies

A named `given instance: TC[T] with` followed by an indented sequence of `def`
members must use the same offside/layout block contract as other body-opening
keywords. The frontend retains the existing `given_obj` AST, and lowering emits
each member under the deterministic `<instance>_<method>` global prefix plus the
instance sentinel used by static method/property dispatch.

This slice does not implement top-level `summon[...]`, anonymous givens, derives,
or type-directed overload selection. Adding `with` to the generic layout opener
is chosen over a given-specific token scanner so explicit braced and indented
bodies share one parser path. A trait-header colon carries a narrow layout bit so
its abstract body receives balanced virtual braces; treating every colon or every
type semicolon as a generic boundary was rejected because the latter exposed
abstract standard-module fields as top-level parser sentinels.

Acceptance:

- [x] a focused VM/direct-ASM fixture covers multiple named givens, zero-argument
  property members, argument-taking methods, and a following top-level statement;
- [x] `typeclass.ssc` executes explicit given calls and advances identically to
  its independent top-level `summon[...]` boundary;
- [x] native-entry, corpus, parity, taxonomy, and fresh conformance pass without
  mismatch/one-sided rows and the typeclass row is honestly reclassified if it
  remains blocked.

Results (`2a223d060`): consecutive indented named givens retain separate
`given_obj` nodes, emit deterministic static globals, and resolve bare sibling
member references without overriding lexical parameters. Both VM and direct ASM
print the focused five-line result; `typeclass.ssc` prints its three explicit
given calls on both lanes and then fails identically at the separately scoped
top-level `summon`. Native corpus remains 194/194 parse+check, 68 classified
sentinels, and 32 successes / 94 honest runtime errors. Strict parity remains 13
identical / 56 both-fail / 126 skipped with zero mismatch or one-sided row;
runtime taxonomy remains 44 blockers, native-entry and taxonomy gates pass, and
fresh affected conformance is 11/11.

#### TI-8.2d2i native math object global

The self-hosted standard program must define `math` as
`IrPrim("__math_obj__", Nil)`, matching the compatibility bridge's CoreIR
contract. This is not a new intrinsic and does not justify a provider plugin:
the v2 VM and direct-ASM runtime already implement the portable math receiver,
including `Pi`, `E`, and numeric method dispatch. The missing piece is only the
self-hosted program definition that makes the receiver reachable.

The definition belongs in the lowerer's fixed kernel globals next to
`println`/collection support, not in the host launcher or v1 bridge. Both lanes
must preserve the existing overload behavior: `abs` retains Int or Double,
`sqrt`/`pow` produce Double, and `round` produces Int.

Acceptance:

- [x] a focused VM/direct-ASM fixture covers `Pi`, `E`, mixed Int/Double `abs`,
  `sqrt`, `pow`, and `round` with deterministic output;
- [x] `enums.ssc`, `imports.ssc`, and `lang-split.ssc` leave the false
  `unbound global: math` boundary and either complete or advance identically to
  a separately reviewed runtime/provider gap;
- [x] native-entry, corpus, strict parity, sentinel/runtime taxonomy, and fresh
  affected conformance pass with no mismatch/one-sided row; only empirically
  removed blockers may tighten the category ceilings.

Results (`ee8467442`): the fixed kernel globals now publish `math` directly from
`__math_obj__`; constants and mixed numeric calls produce the same six-line
result on VM/direct ASM. `enums.ssc` completes, `imports.ssc` advances to its
independent collection-dispatch arity gap, and the explicit
`runScalaFences: true` mixed document is source-classified as a reviewed
compiler/backend surface before execution. Native corpus is 194/194
parse+check, 68 classified sentinels, and 33 successes / 93 honest runtime
errors. Strict parity is 14 identical / 54 both-fail / 127 skipped with zero
mismatch or one-sided row. Runtime taxonomy tightens to 16 language-runtime /
23 standard-provider / 6 optional-provider / 3 example-contract / 6
tools-backend, with 42 blockers and 54 total rows; native-entry, taxonomy gates,
portable gates, and fresh conformance 11/11 pass.

#### TI-8.2d2j exact top-level summon

The self-hosted expression AST must retain the bracket payload of
`summon[TC[T]]` instead of erasing it with ordinary compile-time type
applications. During resolution, the existing named-given table performs an
exact `(TC, T)` lookup and replaces the summon node with that instance global;
ordinary static property/method dispatch then applies unchanged.

This slice does not add reflection, implicit search, derived evidence,
`Mirror.Of`, anonymous givens, subtype matching, or general preservation of
type applications. Missing evidence must remain a deterministic nonzero error
on VM and direct ASM rather than falling back to the compatibility frontend.
Nested brackets in the requested type are retained so the parser boundary is
structurally sound even when the runtime table has no matching evidence.

Acceptance:

- [x] a focused VM/direct-ASM fixture resolves exact named evidence for more
  than one typeclass/type argument and a negative fixture fails identically for
  missing evidence;
- [x] `typeclass.ssc` leaves `unbound global: summon` and either completes or
  advances identically to a separately reviewed language-runtime gap;
- [x] `custom-derives-mirror.ssc` is not falsely marked supported: it either
  remains blocked or advances to explicit Mirror/derives ownership;
- [x] native-entry, corpus, strict parity, sentinel/runtime taxonomy, and fresh
  affected conformance pass with no mismatch or one-sided row before ceilings
  change.

Results (`a5b97f0dd`): the parser retains nested type text only on `summon`, and
the lowerer resolves exact named evidence through the existing given table.
Show/Int, Eq/String, and nested `Label[List[Int]]` fixtures agree on VM/direct
ASM; missing Show/String evidence fails identically. `typeclass.ssc` executes
summon, Eq, Ord, and sorting before advancing to the separately classified
dictionary-sentinel member-dispatch gap. `custom-derives-mirror.ssc` remains
honestly blocked because Mirror/derived evidence is not synthesized. Native
corpus stays 194/194 parse+check, 68 sentinels, and 33/93 runtime; strict parity
stays 14/54/127 with zero mismatch/one-sided row; taxonomy remains 42 blockers,
and native-entry plus fresh conformance 11/11 pass.

#### TI-8.2d2j2 explicit typeclass dictionary dispatch

A named given is an ordinary first-class dictionary value. The self-hosted
lowerer must construct it with the existing target-neutral
`__mk_method_obj__` primitive rather than an integer sentinel, while retaining
the prefixed static globals used by direct and exact-summon calls. Dictionary
properties and method closures therefore remain reachable after the value is
passed, returned, aliased, imported, or stored.

Acceptance:

- [x] an imported multi-file fixture passes distinct dictionaries through
      ordinary parameters and covers properties plus callable members exactly
      on native VM/direct ASM;
- [x] direct and exact-summon access remain unchanged, and `typeclass.ssc`
      completes with exact compatibility output and empty stderr;
- [x] no reflection, host registry, typeclass/example special case, new runtime
      primitive, or compatibility fallback is added;
- [x] all release/parity/taxonomy gates and fresh conformance pass before only
      the empirically completed typeclass blocker is retired.

The complete contract and baseline are in
[`v2.1-typeclass-dictionary-dispatch.md`](v2.1-typeclass-dictionary-dispatch.md).

Results (`8822fa710`, regression `0b596a075`, taxonomy `77da8e8e2`): imported
`given_obj` declarations survive assembly and their globals are the existing
portable method objects rather than integer sentinels. The focused Int/String
value-flow fixture prints nine exact lines and `typeclass.ssc` prints all
eighteen compatibility lines on VM/direct ASM with empty stderr. The full gate
reports 194/194 front/check, 44/82 runtime, parity 31 identical / 35 both-fail /
129 skips with zero mismatch/one-sided rows, 68 sentinel rows / zero standard
gaps, and conformance 11/11. Runtime taxonomy is 10 language / 13 standard / 6
optional / 0 example / 6 tools, 23 blockers / 35 total after the independently
completed HTTP `mount` row was reconciled from the same fresh parity report.

#### TI-8.2d2k nested-pattern arm fallback

Constructor-pattern matching is ordered. If an outer constructor tag matches
but one of its nested constructor obligations does not, evaluation must continue
with the next source arm; it must not throw from the inner `IrMatch`. The source
scrutinee remains evaluated once and is reused through its `IrLet` local.

Nested success scopes contain the real field binders. Nested failure scopes use
dummy binders of the same arity so outer locals and the original scrutinee keep
the same de Bruijn positions without leaking names from a failed arm. Every
additional successful nested match shifts the saved scrutinee position and the
remaining obligations by its field arity. Flat matches retain their existing
compact lowering.

Acceptance:

- [x] focused VM/direct-ASM fixtures cover repeated outer tags distinguished by
  `Some`/`None`, deeper nested constructors, a wildcard fallback, and referenced
  outer locals after nested failure;
- [x] `typed-data.ssc` leaves the false `match: no arm for Some/1|None/0`
  boundary and either completes or advances identically to a separately owned
  language/runtime gap;
- [x] native-entry, corpus, strict parity, sentinel/runtime taxonomy, and fresh
  affected conformance pass with no mismatch/one-sided row before its blocker
  is removed or reclassified.

Results (`b6b359b60`): nested constructor obligations now carry an ordered
failure continuation over the original once-evaluated scrutinee. Success scopes
retain real binders; failure scopes retain only equal-depth dummies, including
multiple nested levels. Repeated `Envelope/2` and `Deep/1` arms distinguished by
Some/None plus wildcard fallback agree on VM/direct ASM. `typed-data.ssc`
completes on both lanes, exposing a separately tracked pre-existing `mkString`
capture bug rather than a match failure. Native corpus improves to 34 successes
/ 92 errors; strict parity to 15 identical / 53 both-fail / 127 skipped with no
mismatch/one-sided row. Runtime taxonomy tightens to 15 language-runtime / 23
standard-provider / 6 optional-provider / 3 example-contract / 6 tools-backend,
41 blockers and 53 total rows; all native-entry/taxonomy/portable/conformance
gates pass (11/11 fresh conformance).

### TI-8.2d2l-n native output and provider integration corrections

Acceptance:

- [x] native `List.mkString` preserves its captured separator for empty,
  singleton, multi-element, and non-String lists on VM/direct ASM;
- [x] the full standard provider set loads without duplicate public `serve`
  ownership while imported `std.ui.serve` retains its reserved ABI-v1 entry;
- [x] an example promoted to identical execution leaves the blocking runtime
  taxonomy and tightens its exact ceilings;
- [x] native-entry, corpus, strict parity, sentinel/runtime taxonomy, portable
  smokes, and fresh affected conformance pass with no mismatch/one-sided row.

Results (`727c806e8`, `23fddc6a2`, `4cdca959c`): `_sel_mkString` now reads the
separator from de Bruijn local 4 rather than the original list at local 5;
`typed-data.ssc` prints `Alice, Charlie` identically on both lanes. NativeUi
registers only `__ssc_nativeui_v1.serve`, leaving the public `serve` global to
the HTTP provider, and `ui-remote-table.ssc` advances from both-fail to
identical. The corpus remains 34 successes / 92 errors; strict parity improves
to 16 identical / 52 both-fail / 127 skipped with no mismatch/one-sided row.
Runtime taxonomy tightens to 15 language-runtime / 22 standard-provider / 6
optional-provider / 3 example-contract / 6 tools-backend, 40 blockers and 52
total rows. NativeUi is 14/14 and all release gates pass, including 11/11 fresh
conformance.

### TI-8.2d2o dynamic selected `toInt`

A zero-argument selected `.toInt` must remain observable when its receiver is
produced dynamically. The self-hosted lowerer therefore routes it through the
portable `__method__("toInt", receiver)` contract instead of erasing every
receiver that is not syntactically recognizable as a String. This preserves
String parsing and the established Int/Float/BigInt/Decimal conversions on the
same VM/direct-ASM dispatch path.

Results (`63ab041a6`): a focused fixture covers a String parameter,
`Option[String].getOrElse`, `None` fallback, and an Int receiver. It prints
`42`, `8`, `1`, `9` identically on native VM/direct ASM. The real
`storage-demo.ssc` counter then advances through its String-to-Int boundary;
native-entry and fresh conformance 11/11 pass.

### TI-8.2d2x dynamic selected `toString`

A selected zero-argument `.toString` uses the Int-only `i->str` primitive only
when its lowered receiver is a proven integer literal. Imported calls,
arithmetic results, parameters, and other dynamically shaped receivers route
through the portable `__method__("toString", receiver)` contract. This keeps
the literal fast path while preserving the existing Int, BigInt, Float,
Decimal, String, and structural fallback semantics in both native engines.

Results (`e2511c6ad`): the focused fixture prints `7`, `1234`, `12.5`, and
`text` identically on native VM/direct ASM and deterministic `build-jvm`. The
full `examples/content-linked-namespaces.ssc` path now resolves the imported
section and prints its title plus BigInt minor units `1234` on all three lanes.
Stage-2 single/multi fixpoints, native-entry, affected conformance 17/17, and
the 195-row strict corpus/parity sweep pass with 45 runtime successes and zero
mismatch or one-sided VM/ASM failures.

### TI-8.2d2p layout object bodies

The self-hosted layout pass now treats a trailing colon as a block opener while
scanning either a `trait` or `object` declaration header. Ordinary parameter,
return-type, and value-ascription colons remain non-openers. The object parser
therefore receives the same virtual-brace structure as an explicitly braced
object instead of dropping its first member and leaking later definitions into
the top-level scope.

Owned methods and parameterless properties share one deterministic prefix.
Property selectors whose receiver parses as an uppercase UID resolve directly
to the owned global, and bare sibling references retain the same owner. The
focused layout/braced fixture prints `40/41/81` twice identically on native VM
and direct ASM.

Results (`afe902ec8`, `b703a6bf0`): all three parser-combinator examples leave
their former missing `Parser_regex`/`Parser_char` boundary and reach the
separately tracked `PRegex/1` constructor-match gap. The 195-file frontend and
checker stay 194/194 green; runtime is 35 successful / 91 controlled errors.
Strict parity remains 22 identical / 44 both-fail / 129 scoped skips with zero
mismatch or one-sided error, and the reviewed runtime taxonomy remains 32
blockers. Native-entry, standard/slim/JRE/core-dependency/build-jvm,
corpus/taxonomy, and fresh conformance 11/11 all pass. Full behavior and exact
release measurements are recorded in
[`v2.1-layout-object-bodies.md`](v2.1-layout-object-bodies.md).

### TI-8.2d2q extension layout and imported ownership

An indented `extension [A](receiver)` now opens a real virtual layout body at
the outer receiver-clause close. Dedent (including the next Markdown code unit)
closes that body before any following top-level definition, so the receiver is
prepended only to actual extension members. Nested receiver type delimiters do
not affect the boundary.

Per-module parser cells are not durable import metadata. The parsed statement
stream therefore retains extension-start/end markers around flat member defs;
the module declaration filter preserves them, and the combined lowerer rebuilds
its selected-extension registry over the assembled closure. This changes the
parser library's `runParser` from erroneous arity 5 to its declared arity 4 and
routes imported `map` to `(global map)` instead of the collection helper
`_sel_map`.

Results (`f7ff66a1f`, taxonomy `4feb715ea`): the exact two-file fixture prints
`20/22/rx/fallback` on VM/direct ASM. All three parser DSLs leave their
`PRegex/1` failure and expose separately tracked symbolic-infix / `case object`
gaps. `dsl-ast-builder.ssc` becomes fully identical, raising runtime success to
36 and strict parity to 23 identical / 43 both-fail / 129 skips with zero
mismatch/one-sided error. Runtime blockers fall from 32 to 31. Every release
gate and fresh conformance 11/11 pass; full behavior and measurements are in
[`v2.1-extension-layout-boundaries.md`](v2.1-extension-layout-boundaries.md).

### TI-8.2d2r symbolic extension precedence

The assembled extension registry now participates in ambiguous symbolic infix
lowering. When `|` is registered, CoreIR carries the exact extension closure,
both evaluated operands, and the operator name through `__arithExt__`.
`IntV | IntV` remains the built-in bitwise operation; every other shape calls
that supplied closure. Without a registered `|`, lowering remains the original
`i.or` path and non-Int misuse stays an honest type error.

Results (`4a336ddec`): the exact two-file fixture prints `a|b/a|b|c/7` on
native VM and direct ASM, and the no-extension negative fixture fails
identically. `dsl-calc-parser.ssc` leaves `PChar(42)` and reaches the separately
tracked `NoContext` case-object gap; `dsl-yaml-like.ssc` leaves `PChar(10)` and
reaches the separately tracked portable `Unit` gap. Full behavior is in
[`v2.1-symbolic-extension-precedence.md`](v2.1-symbolic-extension-precedence.md).

### TI-8.2d2s native case-object values

The self-hosted parser now distinguishes `case object Name` declarations from
match arms and retains them as explicit `caseobj` statements. Optional
inheritance syntax is consumed as type metadata without swallowing the next
top-level declaration. The import-closure filter preserves the statement, and
lowering defines one stable `IrCtor(Name, Nil)` value. Existing structural
equality and nullary pattern matching then work without a provider, intrinsic,
reflection, or compiler fallback.

Results (`500ba1668`, taxonomy `9411ebf0e`): the exact two-file fixture prints
`Empty/empty/true` on native VM and direct ASM. `dsl-calc-parser.ssc` becomes
fully identical; `dsl-json-parser.ssc` advances to the separately tracked
`PMapped/2` match gap, and `dsl-yaml-like.ssc` remains at the separately tracked
portable `Unit` gap. Native front/check is 194/194 with runtime 36 successful /
90 controlled errors. Strict parity is 23 identical / 43 both-fail / 129 scoped
skips with zero mismatch or one-sided errors. Runtime taxonomy is 17
language-runtime / 14 standard-provider / 6 optional-provider / 0
example-contract / 6 tools-backend, 31 blockers and 43 total rows. Full behavior
is in [`v2.1-native-case-objects.md`](v2.1-native-case-objects.md).

### TI-8.2d2s3 parser DSL composed values

An exit-zero parser run is not successful when its mapped value is replaced by
`Stub`. Standard parser combinators, collection composition, tuples, and user
ADTs must preserve the same ordinary portable value on native VM and direct
ASM that the compatibility frontend observes. The fix must be generic
language/runtime semantics, not a JSON/YAML host parser or renderer.

Acceptance:

- [x] an import-shaped first-loss fixture preserves a multi-element composed
      parser value without `Stub`;
- [x] the JSON and YAML parser DSL examples match their exact compatibility
      output on native VM/direct ASM with empty stderr;
- [x] the exact-output parser DSL smoke and all affected release/taxonomy gates
      pass with zero mismatch or one-sided rows.

The complete behavior, rejected alternatives, and baseline are in
[`v2.1-parser-dsl-values.md`](v2.1-parser-dsl-values.md).

Results (`9d5f13f95`, `0b5d1c69c`, `d4cc66736`): imported extensions use the
portable member-first `__methodOrExt__` dispatcher, local tuple destructuring
binds every component, and typed arms preserve nominal tags through
`__isTag__`. Focused composed values and layout output are exact; all seven
JSON and seventeen YAML lines match compatibility on VM/direct ASM with empty
stderr and no `Stub`. The exhaustive release gate reports 194/194 front/check,
40/86 runtime, strict parity 27 identical / 39 both-fail / 129 skips with zero
mismatch/one-sided rows, 68 sentinel rows with zero standard gaps, and fresh
conformance 11/11. The later imported-tuple slice advances current main to
29/37 without changing this contract.

### TI-8.2d2t native typed-pattern boundaries

Match-pattern type annotations now use a dedicated delimiter-depth scanner.
At depth zero it stops before a guard `if` or arm arrow `=>`; nested brackets
and parentheses remain part of the annotation. The general scanner is
unchanged because ordinary function types may legitimately contain `=>`.
This prevents a typed arm body from being swallowed and eliminates the false
empty-arm `uid Unit` value without adding a Unit global or host fallback.

Results (`aef599a80`): the exact imported fixture prints `3/deep/shallow` on
native VM/direct ASM. `dsl-yaml-like.ssc` leaves `unbound global: Unit` and
advances identically to the separately tracked `arity: 0 expected, 1 given`
call gap. Native front/check remains 194/194, runtime 36/90, and strict parity
23 identical / 43 both-fail / 129 skips with zero mismatch/one-sided rows.
Runtime taxonomy remains 31 blockers / 43 total and sentinel taxonomy remains
68 rows with zero standard gaps. Native-entry, portable, standard-tier,
deterministic build-jvm, both taxonomy smokes, and conformance 11/11 pass.
The independently broken HTTP-fast standard staging gates are tracked as
`hf-6`. Full behavior and measurements are in
[`v2.1-native-typed-pattern-boundaries.md`](v2.1-native-typed-pattern-boundaries.md).

### TI-8.2d2t2 extension receiver scope through nested layout

Extension receiver lifetime is now owned by the actual extension body frame,
not by whatever statement a nested parser happens to see. Indented and
explicit-brace frames emit one internal end token at their own close. Nested
match/lambda/block layout closes remain ordinary delimiters and cannot clear
the receiver cell. Receiver-only members after such a body therefore lower as
`lam 1`, parameterized members include the receiver, and a following dedented
top-level definition retains its written arity.

Results (`878474b8d`): the imported layout/braced fixture prints
`2/2/5/3/3/7/9` identically on native VM/direct ASM. YAML leaves the false
arity-zero call, prints `Parsed successfully.`, and joins the separately
tracked JSON `PMapped/2` evaluator boundary. Front/check remains 194/194,
runtime 36/90, strict parity 23/43/129 with zero mismatch/one-sided rows,
runtime taxonomy 31 blockers / 43 rows, and sentinel taxonomy 68 rows / zero
standard gaps. Native-entry, portable, standard-tier, deterministic build-jvm,
taxonomy, and conformance gates pass. The independent HTTP-fast standard-image
packaging gap is tracked separately. Full behavior is in
[`v2.1-extension-receiver-scope.md`](v2.1-extension-receiver-scope.md).

### TI-8.2d2v K62.20 nested flat-tuple pattern compatibility

K62.20 correctly made tuple values of arity three or greater flat `TupleN`
constructors, but the self-hosted pattern parser still produced the obsolete
right-nested `Pair` shape. Nested patterns such as
`Some((left, '+', right))` therefore missed and returned their fallback.
`tuplePat` now follows the same representation rule as values: `Pair` for
arity two and flat `TupleN` for arity three or greater.

Results (`7f6821856`): the established nested tuple fixture again prints
`left/left+right` exactly on VM/direct ASM. The final release baseline is native
front/check 194/194, runtime 35 successful / 91 controlled errors, and strict
parity 22 identical / 44 both-fail / 129 scoped skips with zero mismatch or
one-sided error. Runtime taxonomy is 15 language-runtime / 17 standard-provider
/ 6 optional-provider / 0 example-contract / 6 tools-backend, 32 blockers and
44 total rows; sentinel taxonomy remains 68 reviewed rows with zero standard
gaps. Dependency ownership is 16 roots / 63 static edges / 11 reflective
providers / 30 staged dependency JARs and zero violations. The slim
distribution contains 31 JARs and 6,574 classes (30,541,249 bytes), runs under
13 compiler-free JRE modules, and produces a reproducible 25,778,781-byte
build-jvm artifact with SHA-256
`43ee81cb3699983450b66f838654d7b82884451b2bdab8c30d35d0010705591c`.
Native-entry, standard/slim/JRE/dependency/build-jvm, both taxonomy smokes, and
fresh conformance 11/11 pass.

### TI-8.2d2w portable effect runtime blockers

The self-hosted standard path must preserve the existing target-neutral
`effect.perform` / `effect.handle` computation contract. Ordinary effect
declarations own their exact layout body; operations suspend as explicit `Op`
values; handler partial functions receive unpacked arguments plus a resume whose
multiplicity comes from the declaration (`effect` one-shot, `multi effect`
reusable); pure completion may dispatch `Return`. The raw CoreIR/Mira
`effect.perform` substrate remains reusable. VM and direct ASM must not report an
operation before its enclosing handler consumes it. The later one-shot refinement
uses the additive generic primitive `effect.perform.oneshot(effectId,
operationName, args...)`; it changes neither CoreIR node inventory nor `Op` arity.

Acceptance for TI-8.2d2w1:

- [ ] focused imported fixtures cover declaration boundaries, zero/one/many
      operation arguments, deep one-shot handling, early return, `Return`, and
      reusable multi-shot resume exactly on VM/direct ASM;
- [ ] `effects.ssc` matches all six compatibility lines with empty stderr;
- [ ] the repair reuses portable primitives without v1 bridge/provider/backend
      special cases and leaves standard named runners to TI-8.2d2w2;
- [ ] release/parity/taxonomy and fresh conformance gates pass before only the
      empirically completed row leaves the blocker manifest.

The adjacent `Dataset.of` and recovery `Parser.regex` rows are explicitly split
to TI-8.2d3g and TI-8.2d2x rather than being hidden as generic effect failures.
Full behavior and baseline are in
[`v2.1-native-effect-runtime.md`](v2.1-native-effect-runtime.md).

### TI-8.2d3b core-free Storage effect

`v2/runtime/std/storage-effect-plugin` owns `Storage.get/put/remove/has/keys`,
`runEphemeralStorage`, and deterministic JSON-backed `runStorage`. Each runner
installs a private insertion-ordered map through the generic dynamically scoped
native effect host, so nested scopes restore their outer handler. The persistent
runner selects an explicit path, then `SSC_STORAGE_PATH`, then
`./ssc-storage.json`, loads before the body, and flushes every mutation.

Results (`55aae9abe`, taxonomy `98b0d0976`): provider unit tests are 3/3;
`storage-demo.ssc` has exact output on native VM, direct ASM, and a standalone
`build-jvm` JAR. The 195-file corpus reaches 35 successes / 91 honest runtime
errors; strict parity is 20 identical / 48 both-fail / 127 scoped skips with no
mismatch or one-sided error. Runtime taxonomy tightens to 15 language-runtime /
18 standard-provider / 6 optional-provider / 3 example-contract / 6 tools-
backend, 36 blockers and 48 total rows. Dependency ownership is 14 roots / 54
static edges / 11 reflective providers / 28 staged dependency JARs with zero
violations. Native-entry, ServiceLoader/jdeps/class-load, compiler-module-free
JRE, slim distribution, deterministic build-jvm, taxonomy/portable smokes, and
fresh conformance 11/11 pass.

### TI-8.2d3c core-free reactive signals

`v2/runtime/std/reactive-plugin` owns the general `Signal`, `computed`, and
`effect` globals independently of the NativeUi signal ABI. Public handles are
portable `ReactiveSignal(id)` values; provider-local insertion-ordered maps own
signal values, dynamic effect dependencies, and the synchronous pending queue.
Each rerun drops and recollects dependencies, a source/computed diamond
deduplicates its consumer, and a currently running effect cannot recursively
schedule itself through a signal it writes.

The self-hosted frontend now distinguishes reactive `effect { ... }` calls from
parse-only `effect Name:` declarations. Previously the shared keyword branch
erased both forms, so the assembled `signals-demo.ssc` silently omitted every
effect even though later statements still ran.

Results (`dae51ecab`, evidence `cda669058`, taxonomy `f2ca9b7ea`): provider
tests are 3/3 and the
complete eight-line demo output is identical on native VM, direct ASM, and a
standalone `build-jvm` JAR. The 195-file frontend/checker stays 194 green / 1
non-code and runtime is 35 successful / 91 honest errors. Strict parity is 21
identical / 47 both-fail / 127 scoped skips with zero mismatch or one-sided
error. Runtime taxonomy tightens to 15 language-runtime / 17 standard-provider
/ 6 optional-provider / 3 example-contract / 6 tools-backend, 35 blockers and
47 total rows. Dependency ownership is 15 roots / 58 static edges / 11
reflective providers / 29 staged dependency JARs with zero violations.
Native-entry, provider boundary, compiler-module-free JRE, slim distribution,
deterministic build-jvm, standard-tier, portable/taxonomy smokes, algebraic
effect declaration conformance, and fresh affected conformance 11/11 pass.

### TI-8.2d3d core-free YAML

`v2/runtime/std/yaml-plugin` owns `parseYaml`, `toYaml`, `yamlType`,
`yamlStr`, `yamlNum`, `yamlBool`, `yamlArr`, `yamlGet`, and the private
`__yamlSection__` construction hook. Values cross the native SPI only as
portable `YNull`/`YBool`/`YNum`/`YStr`/`YArr`/`YObj` data; the provider reuses
the dependency-free project `SimpleYaml` subset and never imports a v1 runtime
value or bridge. Serialization sorts object keys and quotes scalar-looking or
multiline strings so nested supported values remain stable after parse/render/
parse.

The self-hosted root/module scanner now tracks the nearest Markdown heading in
document order. A `yaml` or `yml` fence emits a hidden top-level initialization;
selector lowering maps `<SectionId>.yaml` to that binding only when it exists.
This preserves the existing camel-cased heading rule, digit prefixing, multiple
sections, and imported declarations without fabricating a v1 `SectionRuntime`
object.

Results (`2da4183f5`): provider tests are 3/3; `yaml-parse.ssc` is exact on
native VM, direct ASM, and standalone `build-jvm`. Strict parity is 22
identical / 44 both-fail / 129 scoped skips with zero mismatch or one-sided
error. Runtime taxonomy is 15 language-runtime / 17 standard-provider / 6
optional-provider / 0 example-contract / 6 tools-backend, 32 blockers and 44
total rows; the 68-row sentinel taxonomy is unchanged with zero standard gaps.
Dependency ownership is 16 roots / 63 static edges / 11 reflective providers /
30 staged dependency JARs and zero violations. The slim distribution contains
31 JARs including its entry JAR (6,574 classes, 30,532,896 bytes), works under
the 13-module compiler-free JRE allowlist, and the reproducible release artifact
is 25,774,841 bytes with SHA-256
`0e92c739d34ec9cd81426d3e087e6cdebb34f88975c5e2f8b1443a9e54309293`.
All native-entry, provider/class-load, JRE, slim, build-jvm, standard,
portable/taxonomy, and fresh conformance 11/11 gates pass.

### TI-8.2d4 example/config blocker closure

The standard parity harness now recognizes an inline plural front-matter list
containing only JS-family targets, such as `backends: [js, node, wasm]`, as a
reviewed backend-specific program. This is deliberately narrower than treating
every `backends:` key as non-standard: `dataset-parallel-sum.ssc` declares
`backends: [jvm]` and must remain in the standard JVM readiness corpus until its
stack-safety gap is fixed.

The two browser SQL examples already carried the JS/Node/Wasm declaration, so
their previous JVM `both-fail` rows were harness errors rather than example
defects. Conversely, `typed-sql-crud.ssc` already contains its schema fence;
native SQL-fence lowering and typed `Db.insert/update` are absent standard SQL
provider work, not stale fixture setup. Its blocker remains but ownership moves
from `example-contract` to `standard-provider`.

Results (`d4c953b9c`, taxonomy `39cfe268b`): strict standard parity is 21
identical / 45 both-fail / 129 scoped skips with zero mismatch or one-sided
error. Runtime taxonomy is 15 language-runtime / 18 standard-provider / 6
optional-provider / 0 example-contract / 6 tools-backend, with 33 blockers and
45 total rows. The sentinel taxonomy stays 36 backend / 1 nondeterministic / 26
server / 5 tools / 0 standard gaps (68 total). The real browser SQL and JVM-list
focused probes, portable/runtime/sentinel taxonomy smokes, and fresh affected
conformance 11/11 pass.

### TI-2 portable baseline gates

Commands:

```text
scripts/bc-parity-sweep --report "$TMPDIR/v21-bytecode.tsv"
scripts/native-front-corpus --report "$TMPDIR/v21-native-front.tsv"
tests/e2e/v21-portable-gates-smoke.sh
tests/e2e/sscpkg-temp-cleanup-smoke.sh
tests/conformance/run.sh --only 'v2-*'
```

Authoritative 195-file classifications:

```text
bridge VM vs ASM:
  95 identical
  12 both-fail (same failure on both lanes)
  45 backend/target/frontend-specific
   7 nondeterministic-output
  36 server/long-running
   0 mismatch or one-sided error after classification

native front/check/runtime:
  front:    78 OK, 116 ERROR, 1 NON_CODE
  sentinel: 51 PRESENT, 144 clear
  checker:  75 OK, 6 TYPEERR, 113 ERROR, 1 NON_CODE
  runtime:   7 OK, 20 ERROR, 167 NOT_RUN, 1 NON_CODE
```

The VM/ASM run initially exposed ordering-only differences in
`distributed-word-count.ssc`; repeated isolated A/B showed the same word counts
with tie rows in varying order, so the case is explicitly nondeterministic.
`target: js` and scalar `frontend:` files are backend-specific rather than JVM
parity rows. The same sweep found two real defects now tracked in `BUGS.md`:
the native raw-link scanner follows a prose self-link until `File name too long`,
and forced bytecode execution of `x402-metamask.ssc` can turn an unresolved
wallet call into a successful top-level `Op`. Neither is hidden by the TSV.

Verification: portable-gates e2e PASS, sscpkg cleanup e2e PASS,
`SscpkgLoaderTest` 12/12, and affected v2 conformance 8/8.

### TI-3 staged native production entry

`installBin` now stages seven self-hosted tower files plus 100 `.ssc` standard
modules under `bin/lib/native-front/`. `RunV2.runNative` executes the tower in
process on the prebuilt v2 kernel; only that frontend thread receives a 64 MiB
stack, so long-running server threads keep the normal JVM stack. The route:

```text
ssc run --native sources... -- argv...
  -> staged ssc1-run.ssc0 (standalone Markdown imports only)
  -> normalized DFS module loading + staged std root
  -> CoreIR, reject `(global _err)`
  -> plugin-enabled v2 VM

ssc run --native --bytecode ...
  -> the same native CoreIR -> direct ASM execution
```

The prose self-link loop is closed: `components-demo.ssc` now reaches the bounded
parser-sentinel diagnostic instead of recursively building `././...` until
`File name too long`, and its larger frontend run no longer overflows the CLI
thread stack. `RunV2` also registers extracted plugin JAR children for reverse
shutdown deletion, so neither `sscpkg-*` nor `ssc-v2-plugins*` trees survive.

Verification:

```text
scripts/sbtc "cli/compile; installBin"
V2RunArgvCliTest: 2/2
tests/e2e/v21-native-entry-smoke.sh: PASS
tests/e2e/sscpkg-temp-cleanup-smoke.sh: PASS
tests/conformance/run.sh --only 'v2-*': 8/8
```

The native-entry smoke sanitizes `PATH` so `scala-cli` is absent and covers
hello, a relative import containing a prose self-link, multiple source files,
argv, staged `std/os.ssc`, staged `std/crypto.ssc` plus its `sha256` plugin
intrinsic, VM, ASM, explicit compatibility routing, and sentinel rejection.

### TI-5 native FS/OS providers

`v2/runtime/std/{fs,os}-plugin` now implement the standard JVM file-system,
environment, path, and temporary-file surface directly against `ssc.Value` and
`NativePluginContext`. They depend only on the v2 runtime/SPI graph. The host
provider retains the process globals, avoiding duplicate ServiceLoader
ownership when all standard providers are present.

Verification:

```text
scripts/sbtc "v2NativePluginSpi / Test / test; v2NativeCryptoPlugin / Test / test; v2NativeOsPlugin / Test / test; v2NativeFsPlugin / Test / test"
NativePluginHostTest 3/3; CryptoNativePluginTest 2/2; OsNativePluginTest 2/2; FsNativePluginTest 2/2
scripts/sbtc "installBin"
tests/e2e/v21-native-entry-smoke.sh: PASS (FS/OS VM + direct ASM)
tests/e2e/v21-native-plugin-boundary-smoke.sh: PASS (ServiceLoader + jdeps + runtime class trace)
tests/conformance/run.sh --only 'v2-*': 8/8
```

### TI-5 native HTTP client and Response values

`v2/runtime/std/http-plugin` depends only on the native SPI and JSON provider.
Outbound requests use `java.net.http.HttpClient`; `Response` is the ordinary v2
data constructor with registered `status`, `headers`, and `body` fields. The
provider owns `Response.*`, cache helpers, client configuration, and outbound
stream callbacks. Server operations are registered as bounded failures until
the server-host SPI from §3.3.2 is available, preventing a compatibility
classpath edge or false-success registration.

`a81d9d94f` extends that provider with a JDK server-host implementation and the
general `NativePluginContext.invoke` trampoline seam. The initial host supports
exact method/path routes and lifecycle; advanced middleware, TLS, streaming,
SSE, upload, and WebSocket hooks remain explicit incomplete behavior rather than
no-op compatibility shims.

Verification:

```text
scripts/sbtc "v2NativePluginSpi / Test / test; v2NativeJsonPlugin / Test / test; v2NativeHttpPlugin / Test / test": 4/4 + 3/3 + 4/4
scripts/sbtc "installBin": 102 runtime JARs, 7 native-front tower files, 100 std modules
tests/e2e/v21-native-entry-smoke.sh: PASS (Response/cache and JDK route loopback VM + ASM; advanced-server negative)
tests/e2e/v21-native-plugin-boundary-smoke.sh: PASS (ServiceLoader + jdeps + runtime class trace)
tests/conformance/run.sh --only 'v2-*': 8/8
```

### TI-5 native JSON provider

`runtime/std/json-core.ssc` is the canonical strict/tolerant scanner, lossless
target-independent ADT, total navigation layer, and deterministic compact
renderer. `std/json.ssc` exposes that implementation publicly on both native VM
and direct ASM. The small `v2/runtime/std/json-plugin` is now only a core-free
`ssc.Value` bridge for portable `JsonCore*` ADTs and navigation methods; it has
no parser, renderer, ujson, or upickle dependency. `jsonValue` remains
deliberately tolerant; `jsonParse`/`jsonRead` remain strict. `jsonStringify`,
structured builders, navigable `.raw`, and HTTP `Response.json` all use the
installed ScalaScript renderer, so JSON and HTTP remain operational after the
external codec family is physically removed.

Verification:

```text
scripts/sbtc "v2NativeJsonPlugin/test;v2NativeHttpPlugin/test": PASS (3/3 + 4/4)
tests/e2e/v21-self-hosted-json-core-smoke.sh: PASS
tests/e2e/v21-self-hosted-json-cutover-smoke.sh: PASS
tests/e2e/v21-native-entry-smoke.sh: PASS (JSON/builders/HTTP on VM + direct ASM)
tests/e2e/v21-native-plugin-boundary-smoke.sh: PASS (ServiceLoader + jdeps + runtime class trace)
tests/e2e/v21-slim-distribution-gate.sh: PASS after deleting ujson/upickle/upack/geny
tests/conformance/run.sh --only 'json-*' --no-memo: 3/3
tests/conformance/run.sh --only 'v2-*' --no-memo: PASS
```

### TI-8.2d3l core-free process-local Graph provider

`v2/runtime/std/graph-plugin` owns the portable `Graph.*` property/RDF surface
for the standard VM and direct ASM lanes. Stores are process-local and
insertion ordered; vertices replace by structural `id`, edges validate both
endpoints and receive deterministic IDs, neighbor traversal returns stored
destination values, and RDF records/triples retain portable data values.
`Sparql.*`, `Cypher.query`, and `Gremlin.query` remain explicit external
capabilities and fail with bounded provider diagnostics locally. No operation
routes to the v1 graph plugin or compatibility frontend.

Results (provider `eb69124e2`, taxonomy `ff42d5d57`): provider tests are 4/4;
`graph-storage-interpreter.ssc` is exact on VM/direct ASM/build-jvm and
`graph-rdf4j-http-storage.ssc` reaches the same explicit `Sparql.select`
boundary after two portable writes. Only the local blocker is retired; the
remote RDF4J HTTP row is a reviewed non-blocking optional-provider capability.

### Self-hosted core release baseline (2026-07-12)

The standard language core has zero external JSON/YAML/Markdown/parser
dependencies. `runtime/std/json-core.ssc` remains the canonical JSON scanner,
ADT, navigation, and renderer; its native plugin is only the core-free
`ssc.Value` bridge. Frontmatter/content compilation is self-hosted and crosses
the Scala seed as structural `NativeCompilation` values, so the retired host
`NativeFrontmatter`/`SimpleYaml` path is not loaded. The separate standard YAML
runtime still owns the documented project `SimpleYaml` subset as a feature
provider dependency; it is neither an external parser nor a frontend fallback.
The installed 112-file compiler image is source-exact and both single-file and
multi-file images are stable stage-2 fixpoints.

```text
scripts/v21-self-hosted-core-release-gate --skip-install: PASS
  compiler.image.files: 112; compiler.image.source-exact: true
  compiler.image.sha256: bd055ff4034fa87e672c5e7e670171ec15961bf238fbe0df5e118d023560ee1a
  dependency roots/edges: 25 / 101; reflective providers: 11
  standard.dependency-jars: 39; outside ownership: 0
  dependency.strict-parser-violations: 0
  external-parser-jars.standard: 0
  frontend corpus: 194 OK + 1 non-code; checker: 194/194
  VM/ASM: 53 identical / 13 both-fail / 129 scoped skips
  VM/ASM mismatch: 0; one-sided backend errors: 0
  sentinel: 24 backend / 1 nondeterministic / 24 server / 5 tools / 0 gaps
  runtime taxonomy: 0 language / 0 standard / 7 optional / 6 tools
  blocking both-fail: 0; total runtime-classified: 13
  exact release freeze: 7 optional / 6 tools / 0 blockers / 13 total
  default launcher: standard; compatibility entry: explicit ssc-tools
  slim: 40 jars / 6,684 classes / 31,178,916 bytes; tools absent
  reproducible artifact: 26,916,912 bytes
  artifact sha256: 57e16a781435aad5cf57b5837542f3e776ece7e9b2b2355285e1ed97c7131909
  conformance: 11/11; corpus.full: pass; release.ready: true
```

The delta from the immediately preceding Graph baseline is the exact native
closure of `dsl-mini-language.ssc`: source parameterless-def value semantics
and the existing tuple-callback adaptation now preserve portable `Pair/2`.
The standard path, not compatibility fallback, produces the 13 canonical lines
on VM, direct ASM, and reproducible `build-jvm`.

The next delta closes `custom-derives-mirror.ssc` in the self-hosted language
tower. Product field type spellings and derives clauses now survive parsing;
exact generated given keys point to portable `Mirror/3` data and one
source-ordered `TC.derived` result. The four-line public output is identical on
VM/direct ASM/build-jvm without loading v1 `DerivesRuntime`, Scala reflection,
or the compiler tier.

The following delta closes explicit `direct[Option]` and `direct[List]` on the
self-hosted frontend. The parser preserves a dedicated direct node and the
lowerer rewrites fresh binds to portable `flatMap`, while pure `val`, mutable
`var`, nested blocks, Option short-circuiting, and List source order remain
observable. Unsupported monads fail through a bounded standard diagnostic;
there is no v1 `DirectAnorm`, runtime primitive, or transparent fallback.

The next independent delta preserves case-class named-copy labels through the
self-hosted frontend/runtime seam. Receiver and overrides are bound once in
source order before portable `copy` dispatch rebuilds the record from registered
field metadata. The focused VM/direct-ASM/build-jvm fixture is exact, while the
public lenses row remains honestly blocked at `Focus`; parity and taxonomy
therefore remain 50/16 and three blockers without a compatibility fallback.

The final language delta preserves `Focus[T](_.path)` and
`Prism[Outer, Variant]` structurally in the self-hosted frontend and installs a
core-free optics provider. Field, `.some`, and `.each` steps are portable data;
arbitrary getter expressions fail through a bounded sentinel and never execute
via macro/reflection or compatibility fallback. All 23 public lenses lines are
exact on standard VM, direct ASM, slim/JRE, and reproducible `build-jvm`.
Consequently the language-runtime taxonomy reaches zero; only the declared SQL
standard-provider blockers remain at this checkpoint.

The next provider delta retains raw `sql` fences in exact document order,
rewrites `${expr}` to source-ordered JDBC binds (`$$` remains literal), and
binds every `_sqlBlock_N` plus the first `<Section>.sql` alias through the
core-free SQL provider. The public H2 quickstart is exact on standard VM,
direct ASM, slim/JRE, and reproducible `build-jvm`; malformed/client-only fences
fail before stdout, and attributed client ScalaScript blocks retain their prior
JVM exclusion semantics. This advances parity to 52/14 and leaves exactly one
typed CRUD standard-provider blocker without any compatibility fallback.

The final provider delta preserves the nominal argument of `Db.query[A]`,
derives a bounded portable row schema from `Mirror`, and implements fully bound,
identifier-validated `Db.insert`/`Db.update` plus case-insensitive product row
reconstruction. Nullable `Option` values and bounded negative diagnostics are
covered in the provider and installed lanes. `typed-sql-crud.ssc` is exact on
VM/direct ASM/slim/JRE/reproducible `build-jvm`, advancing parity to 53/13 and
reducing blocking both-fail to zero. The remaining 13 rows are frozen reviewed
optional-provider or tools-backend surfaces, not standard fallback candidates.

TI-8.2d5 makes that snapshot executable rather than documentary. The release
gate now compares the generated runtime taxonomy to an exact data snapshot and
fails on count movement in either direction, blocker/total drift, missing or
duplicate metrics, stale membership, or reclassification. Its self-test proves
shrink/growth/blocker failures, and the full summary pins 7 optional, 6 tools,
and 0 blockers before declaring `release.ready=true`.

TI-8.3 makes that release-ready graph the default. `bin/ssc`, generated
`installBin`/contributor launchers, and self-installed `ssc` now select the
class-filtered standard JAR and `StandardMain`; `ssc-standard` remains an
equivalent alias. StandardMain no longer contains a `ProcessBuilder` tools
delegation, so `--v1`, `--compat-frontend`, and compiler/backend commands fail
before source execution and name explicit `ssc-tools`. The compatibility
conformance lanes themselves now invoke `ssc-tools`, while v2 VM/direct ASM use
plain `ssc`. The exhaustive release stays 53/13/129 with zero blockers and
fresh conformance 11/11; the Scala 3 seed and artifact SHA are unchanged.

TI-9 proves the same result under removal rather than convention. The combined
negative gate copies only `ssc`, `ssc-standard`, and `lib/standard/**`; hides
scala-cli/scalac/javac; limits Java to the statically derived runtime set plus
the reflective `jdk.crypto.ec` JCA provider; and re-runs all 195 frontend/check
and VM/ASM rows plus JSON, SQL, state, Ed25519, and HTTP server smokes. Its
frozen report is 194/194, 53/13/129, zero mismatch/one-sided/blocker, zero
compiler/Scalameta JARs or forbidden references, both compiler modules absent,
and `release.ready=true`. A synthetic validator test proves launcher, JAR,
command, module, frontend, parity, blocker, and report-shape drift is rejected.
