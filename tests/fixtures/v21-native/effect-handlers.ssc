---
name: v21-native-effect-handlers
---

[Probe, MultiProbe, afterDeclaration, zeroArgument, oneArgument, manyArguments, ignoredResume, deepResume, returnArm, multiShot](effect-handlers-lib.ssc)

# Imported portable effect handlers

```scalascript
println(afterDeclaration())
println(zeroArgument())
println(oneArgument())
println(manyArguments())
println(ignoredResume())
println(deepResume())
println(returnArm())
println(multiShot())
```
