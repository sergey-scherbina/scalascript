feature/v2-f5b-typed-ir 2026-07-22T03:59:39Z
agent: opus-orch
heartbeat: 2026-07-22T14:28:08Z
status: in-progress
done-so-far: v2-f5c STEP 1 = the DEFAULT ssc run backend is now the JVM-bytecode lane (c05924863, on origin/main 9ddd2b501). StandardMain default VM->bytecode + reversible opt-out --interpret/--vm/SSC_EXEC=vm. The FLIP GATE (F4 lesson: corpus green != sufficient) CAUGHT + FIXED a landed regression: fix (a) 7332deb05 — SLICE-1 matched ASM size errors BY TYPE, eagerly loading ASM on the VM path (v21-plugin-backend-isolation RED on main since 48e31b163); now matched by class-name. + b1: isolation smoke VM checks forced --interpret. GATES: switch-relevant e2e A/B (bytecode default vs SSC_EXEC=vm) 0 REGRESSIONS (both-fail smokes fail on VM too = pre-existing/env); examples DIFF=0/BC-FAIL=0; pattern-match-heavy 500k+3M no overflow; effectful byte-identical; semantic 248/248; fixpoint byte-identical; conformance slice 9/9 all backends; v2JvmBytecode/compile. Reversible.
next: (1) VERIFY exact-SHA CI on the switch (currently UNKNOWN — async; per plan CI is the out-of-corpus net, revert if red). (2) f5c-3 = typed f.* double + lcell/dcell accumulator i.* recognition in JvmByteGen (2 remaining __arith__-only sites) so float/accumulator numeric is fast on the default lane. (3) f5c-4 = FastCode/SelfRec removal (~-1186 L from Runtime.scala) on a CI-VERIFIED switch. STOPPED after STEP 1: the removal is the highest-stakes (irreversible kernel deletion) and must build on a CI-green switch, not be stacked at the end of one increment.
