slug: front-braceless-try-def-body
branch: feature/front-braceless-try-def-body
agent: opus
started: 2026-07-27T17:45:00Z
heartbeat: 2026-07-27T17:45:00Z
direction: Language & Compiler (front / layout)
scope: |
  BUGS v2-front-try-in-def-body-shapes-break shape (b) — the half left open after (a) landed
  (11d976cba): a BRACELESS MULTILINE `try` as a def body does not terminate cleanly, so what follows
  it is corrupted — a following top-level statement is silently dropped (exit 0), a following def
  whose body starts with `try` becomes `unbound global: try`.
  FIRST STEP IS RE-MEASUREMENT, not the old note: the 2026-07-18 entry has already been wrong twice
  this session (it called layer 2 "not diagnosed" and layer 1 "still broken" when legacy had fixed
  it), and my own (a) fix may have moved the picture. Establish per-front/per-shape truth first.
  Known lead: lowering `def w(..) = try { .. } catch { .. }` through the LEGACY lowerer produced
  `(app (global try) ..)` — i.e. legacy parses `try` as an IDENTIFIER when it is a def body, which
  is a different defect from F's. ssc1-front.ssc0 try arm ~:1076-1106; F's parseTry ~:1852.
