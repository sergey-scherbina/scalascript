jit-loop-val-extensions 2026-06-13T20:45:00Z
agent: claude-opus-4-8
heartbeat: 2026-06-13T20:45:00Z
status: in-progress
done-so-far: jit-loop-tuple (tupleMonoid 2663×) + jit-loop-val-inline (valIntermediate 9124×, + reassign-before-use soundness guard) shipped. collectFastAssignBody now inlines ONE leading pure-arith/tuple val. This extends it.
next (autonomous, the loop-JIT lever — user: "do everything you can until optimal"):
  A1 MULTIPLE/CHAINED leading vals — currently a 2nd leading Defn.Val bails (fromAssigns hits non-Assign). Peel ALL leading vals, compose via substVal (chaining: substitute prior vals into each later val's expr), then apply to assignments. All vals PURE (tuple/pure-arith) so no side-effect ordering risk; reassign guard per-used-val. High real-code value.
  A3 DESTRUCTURING `val (a,b) = (e1,e2)` — Pat.Tuple binding; bind each to the tuple element (pure).
  A4 Double-typed pure vals — verify `val d = x*1.5` routes to the Double while-JIT (may already work).
  DEFER A2 (single-use call vals) — side-effect ORDERING risk (reorders f past intervening stmts); needs side-effect analysis, not just the var-reassign guard.
GATE: real A/B win (add bench modMultiVal etc.); TupleScalarReplaceTest + 521 interp tests green; full bench no-regression. SAME GOTCHAS: scalameta no Tree.transform; sbt incremental stale (clean); AST loop-inline MUST check captured-expr vars not reassigned before use.
