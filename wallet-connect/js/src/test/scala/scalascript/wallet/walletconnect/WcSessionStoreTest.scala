package scalascript.wallet.walletconnect

/** Scala.js concrete subclass — pure-Scala spec, no `CryptoBackend`
 *  setup needed (`WcSessionStore` does no crypto). */
class WcSessionStoreTest extends WcSessionStoreTestBase
